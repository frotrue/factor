# 파일 역할 맵

프로젝트 이해와 구현에 중요한 파일만 압축 정리했습니다. 전체 구조는 [PROJECT_MAP.md](./PROJECT_MAP.md), 런타임 흐름은 [ARCHITECTURE.md](./ARCHITECTURE.md)를 참고하세요.

| 파일 | 역할 | 관련 흐름 | 수정 시 주의점 | 관련 테스트 |
|---|---|---|---|---|
| `src/main.ts` | Phaser 게임 생성, `window.__GRADIUM_GAME__` 노출, 기본 FPS target 설정, Preact HUD overlay 마운트 | 앱 시작, Playwright 상태 조회, 성능 기본값, DOM HUD 점진 이전 | Scene 배열 순서가 메뉴->게임 흐름을 결정. 저장된 FPS 설정은 clamp되며 기본값은 60 FPS. Preact는 DOM overlay만 담당하고 Phaser canvas 렌더링은 유지 | `tests/e2e/app-smoke.spec.ts` |
| `src/scenes/MainMenuScene.ts` | 난이도 선택 메뉴, Preact MainMenu snapshot 발행과 menu request 처리 | 메뉴 UI, 난이도 전달, 저장 슬롯 감지, 이어하기 start request | Phaser 배경/파티클은 유지하고, 보이는 메뉴는 Preact overlay가 담당한다. MainMenu legacy fallback 입력과 Preact snapshot 공통 display payload는 `src/ui/mainMenuDisplay.ts`에 위임하며, overlay aria label도 snapshot에서 공급한다. Legacy Phaser 텍스트/입력 hit target fallback 생성은 `src/ui/legacyMainMenuFallback.ts`에 위임한다. E2E가 기다리는 text 객체와 시작 좌표 fallback을 보존하고, Preact overlay request도 기존 start/difficulty 흐름으로 처리. 이어하기는 `loadSave` 플래그만 전달하고, legacy gameplay HUD display 복원은 `src/ui/legacyHudDom.ts`에 위임 | `tests/e2e/app-smoke.spec.ts` |
| `src/scenes/MainScene.ts` | 게임 런타임 조립, 매니저 초기화, 프레임 update, 이벤트 연결 | 거의 모든 게임 흐름의 중심, menu continue load flag 처리 | 매니저 초기화 순서, EventBus owner cleanup, 모바일 layout 상태 주의. mobile media query/body class 조작은 `src/ui/domEnvironment.ts`에 위임한다. `loadSave`는 campaign 시작 후 기존 `SaveManager.loadGame()`만 호출 | E2E 전반 |
| `src/config.ts` | 건물/아이템/레시피/적/연구/난이도/타이밍 설정 | 밸런스, UI, 팩토리, 테스트의 단일 원천 | 새 ID 추가 시 `types.ts`, `BuildingFactory`, i18n, 테스트 동시 갱신 | `src/config.test.ts`, 다수 utils 테스트 |
| `src/types.ts` | 핵심 타입, 저장 포맷, Scene 인터페이스 | 타입 계약, GameMode, SaveData, manager 접근 | SaveData 변경 시 `SaveManager`와 migration 필수. `mode`는 튜토리얼/캠페인 실행 경계, `settings.mapType`은 저장 맵 복원 힌트 | `src/utils/saveMigration.test.ts` |
| `src/i18n.ts` | 한국어/영어 번역과 언어 저장 | 메뉴, HUD, 툴팁, 테스트 텍스트 | 새 UI 키 추가 시 ko/en 모두 추가 | `src/i18n.test.ts`, E2E language smoke |
| `src/styles/main.css` | 앱 전역 리셋과 Phaser mount 기본 스타일 | body, `#game-container`, legacy/Preact 공통 CSS 변수 | Preact/legacy 세부 UI 스타일을 넣지 않는 얇은 전역 파일. canvas touch-action과 body overflow 변경은 입력/E2E에 영향 | build, E2E startup |
| `src/styles/legacy-ui.css` | 기존 DOM fallback과 short-landscape 호환 스타일 | `legacyHudDom.ts`가 생성하는 PC HUD shell, 우측 정보 레일, 하단 빌드 콘솔, 모달, 모바일 액션바 | DOM id/class와 Playwright 셀렉터 영향. 데스크톱에서는 legacy top/right/build shell이 `data-preact-shadow` hidden fallback이고, mobile layout에서는 legacy compact fallback이 다시 보인다. 모바일에서는 HUD가 터치 배치를 가로막지 않게 pointer-events 주의 | `tests/e2e/app-smoke.spec.ts` |
| `src/styles/tokens.css` | Preact UI 이전을 위한 전역 디자인 토큰과 `#preact-hud` overlay root layer | 새 HUD 컴포넌트, 기존 CSS 변수 보강, Preact overlay stacking root | 팔레트/간격/타이포그래피/그림자 변수와 Preact root positioning만 제공하고 게임 로직과 분리. `#preact-hud`는 fixed/pointer-events none으로 canvas 입력을 막지 않음 | build, E2E visual smoke |
| `src/ui/mountHud.tsx`, `src/ui/HudApp.tsx` | Preact HUD overlay 마운트와 최상위 조합 | Phaser Game 생성 이후 DOM HUD root 렌더 | `#game-hud-shell`은 빈 legacy container로 남고, `#preact-hud` root는 legacy HUD보다 높은 overlay layer이며 pointer-events 기본 none을 유지한다. 각 button/modal/mobile surface만 pointer event를 받음 | `npx tsc --noEmit`, build, E2E |
| `src/ui/signals/gameState.ts`, `src/ui/signals/bridge.ts`, `src/ui/signals/notificationState.ts`, `src/ui/signals/tutorialState.ts`, `src/ui/signals/mobileState.ts`, `src/ui/signals/menuState.ts` | EventBus -> Preact signals 단방향 menu/HUD/notification/tutorial/mobile 상태 브릿지 | menu, score/power/silicon/packets/wave/timer/TopBar labels 표시 상태, wave result toast/history/activity log/tooltip/tutorial/mobile action 상태 | simulation manager를 직접 읽거나 변경하지 않음. 기존 scene/manager들의 dual-publish 이벤트를 받아 DOM overlay만 갱신. CORE/POWER/wave start/timer direct events와 languagechange label refresh도 `topHudDisplay.ts` snapshot helper를 사용해 TopBar 포맷을 legacy HUD helper와 맞춘다. Settings/Training Lab/WaveResult open-only events는 각 display helper의 snapshot open-state patch helper를 사용한다. Wave result close는 Preact signal만 닫고 history는 최근 snapshot 5개만 표시용으로 보관. tooltip close는 기존 `UIManager.hideTooltip()` 경로로 돌아감 | `npx tsc --noEmit`, build, E2E |
| `src/ui/components/MainMenu.tsx` | Preact main menu overlay | MainMenuScene difficulty/start/continue/tutorial/save snapshot 표시, keyboard shortcut, labeled/described difficulty button group, focusable difficulty/start/continue controls, stable Preact test IDs, request 이벤트 | 보이는 메뉴 UI를 담당. `#preact-main-menu-start`는 E2E 시작 클릭 대상이며 `#preact-main-menu-continue`는 저장 슬롯이 있을 때만 표시. Difficulty controls keep button semantics, expose selected state with `aria-pressed`, sit inside a snapshot-labeled group, and describe the active option with `preact-main-menu-difficulty-description`; Arrow/Enter/C keyboard shortcuts도 기존 MainMenuScene EventBus request만 발행. Tutorial/save status strip uses polite live updates, and continue label, key hints, difficulty descriptions는 snapshot의 i18n 표시 문자열만 렌더하고 실제 scene 전환과 save load는 MainMenuScene/MainScene 경로가 처리 | E2E startup smoke |
| `src/ui/mainMenuDisplay.ts` | MainMenu display snapshot helper | menu aria/title/subtitle/start/continue labels, difficulty options/descriptions, tutorial/save status, keyboard hint labels를 legacy fallback 입력과 `MAIN_MENU_UPDATED`용 Preact snapshot 공통 display payload로 변환 | Phaser menu background/particles, scene transition, difficulty selection, save loading은 하지 않는다. `MainMenuScene`의 selected difficulty와 storage/language 상태를 legacy/Preact 표시 payload로만 바꾼다 | E2E startup smoke |
| `src/ui/legacyMainMenuFallback.ts` | 기존 Phaser main menu text/input fallback helper | invisible Phaser title/subtitle/difficulty/start text objects and click hit targets | Preact MainMenu가 실제 표면이지만 canvas coordinate fallback과 기존 smoke 안정성을 위해 alpha 0.001 hit target을 유지한다. 실제 start/difficulty 상태 변경은 콜백으로 `MainMenuScene` request 경로에 되돌린다 | E2E startup smoke |
| `src/ui/components/TopBar.tsx`, `src/ui/shared/StatCard.tsx` | 새 Preact 상단 HUD 바와 공통 stat card | 데스크톱 실제 리소스/웨이브 표시, snapshot 기반 i18n label/aria rerender, 값 변경 data-scan feedback, keyboard focusable settings/research/Lab shortcut request | 기존 `#top-hud`/`#hud-top-bar` DOM ID는 attached hidden shadow fallback으로 보존하고 Playwright 호환 텍스트 sync를 유지한다. shortcut/stat labels and HUD aria labels come from the `topHudDisplay.ts` label snapshot. shortcut은 `SETTINGS_OPEN_REQUESTED`, `RESEARCH_OPENED`, `TRAINING_LAB_OPEN_REQUESTED`만 발행하고 실제 modal open은 기존 manager 경로가 처리. StatCard는 `role=list/listitem`, stable `preact-stat-*` test IDs, label/value linkage를 노출하고 animation은 표시 전용 local state이며 reduced motion을 존중. root는 labeled HUD section이고 버튼만 pointer-events auto. TopBar z-index는 legacy info layer보다 높고 modal overlay보다 낮게 유지 | E2E startup/settings/modal smoke |
| `src/ui/topHudDisplay.ts` | Top HUD display helper | score/packets/silicon, `PowerUpdateData`, wave start/timer, TopBar localized labels를 legacy top HUD payload/Preact HUD snapshot으로 변환하고 resource/power/wave-start/wave-timer/label refresh는 공통 display payload 또는 snapshot helper로 조립 | 리소스/전력/웨이브 이벤트 데이터와 localized labels를 상단 HUD 표시 payload로만 바꾼다. Inventory, PowerManager, WaveManager, timer 계산은 변경하지 않음 | E2E startup/topbar smoke |
| `src/ui/legacyTopHud.ts` | 기존 상단 HUD DOM fallback helper | `#hud-score`, `#hud-power`, `#hud-wave`, `#hud-wave-timer`, `#hud-packets`, `#hud-silicon` mirror text/style update | Preact TopBar가 실제 표면이고 이 파일은 legacy stat IDs의 hidden shadow fallback만 갱신한다. EventBus `HUD_STATE_UPDATED` 발행과 값 변경 throttle은 `UIManager`가 유지하고, simulation manager를 직접 참조하지 않음 | E2E startup/topbar smoke |
| `src/ui/components/RightRail.tsx`, `src/ui/shared/GlassPanel.tsx`, `src/ui/shared/ProgressBar.tsx` | 새 Preact 우측 전술 레일과 공통 패널/진행바 래퍼 | objective/threat/defense/power status 표시, snapshot-localized rail/collapse/section/power-load labels, 전력 부하 progressbar, keyboard focusable panel collapse, stable Preact panel/body/toggle/threat/power meter test IDs, threat progressbar/routes 표시, tactical bevel/scanline panel shell | 데스크톱에서는 Preact rail이 실제 표면이고 기존 `#hud-right-rail`/`mission-panel`/`threat-panel`/`systems-panel`은 attached hidden shadow fallback으로 동기화된다. Each RightRail panel is a labeled `role=region` with stable body IDs, threat routes expose list/listitem semantics, and the power load meter uses `preact-right-rail-power-load`. threat meter는 `WaveBriefing` snapshot 메타를 시각화하고 `aria-valuetext`로 threat level을 노출만 하며 웨이브 로직을 계산하지 않음. Shared ProgressBar exposes clamped `aria-valuenow`/`aria-valuetext` from display-only values and does not drive power/training calculations. collapse는 Preact local UI state이고 `aria-expanded`/`aria-controls`를 유지. GlassPanel scanline/alert pulse는 Preact DOM overlay 전용이며 `prefers-reduced-motion`을 존중. 모바일/좁은 화면에서는 Preact rail을 숨겨 조작과 기존 HUD를 방해하지 않음 | E2E startup/perf smoke |
| `src/ui/tacticalPanelDisplay.ts` | Tactical panel display helper | objective state, `WaveBriefing`/timer, `PowerUpdateData`, defense model counts/state를 localized labels 포함 legacy tactical panel 표시 payload와 `TACTICAL_PANELS_UPDATED` common display payload/snapshot으로 변환 | 목표/웨이브 브리핑/전력/방어 모델 상태 계산 결과를 RightRail/legacy fallback 표시 텍스트와 tone으로만 바꾼다. RightRail snapshot은 DOM fallback 텍스트를 다시 읽지 않고 같은 표시 payload에서 생성한다. WaveManager, PowerManager, TrainingPlanner, progression gate 판단, 적 스폰, 난이도 계산은 변경하지 않음 | E2E startup/right-rail smoke |
| `src/ui/legacyTacticalPanels.ts` | 기존 우측 tactical DOM fallback refs/update helper | mission/threat/systems panel refs 조회, panel text, wave timer, power chip tone 반영 | 목표/웨이브/방어/전력 계산은 `UIManager`가 유지하고, 이 파일은 legacy DOM 조회/반영만 담당. 기존 `mission-panel`/`threat-panel`/`systems-panel` IDs와 `panel-chip-*` classes 유지 | E2E startup/right-rail smoke |
| `src/ui/components/BuildConsole.tsx`, `src/ui/shared/Button.tsx`, `src/ui/signals/buildState.ts` | 새 Preact 하단 빌드 콘솔과 공통 버튼 primitive | active category, build item list, selected tool 표시와 선택/카테고리 요청, snapshot-localized aria/preview/command labels, hover/focus tool preview, keyboard focusable tab/tool controls, tab-to-tool-panel linkage, stable Preact test IDs/click targets, hotkey command strip, mobile portrait compact rail | 실제 상태 변경은 EventBus request를 받은 `UIManager`가 담당. 데스크톱과 mobile portrait에서는 기존 `#bottom-ui-container`/`#build-console`/`#ui-overlay` DOM console이 attached hidden shadow fallback이고 Preact BuildConsole이 실제 표면이다. Short landscape에서는 세로 공간 때문에 Preact BuildConsole을 숨기고 legacy fallback을 계속 사용한다. available building PNG는 icon으로 쓰고 없는 항목은 color swatch fallback을 유지. preview와 command strip은 `CONFIG.BUILDINGS`/`CONFIG.CABLES` read-only 메타 및 기존 hotkey 계약 표시만 담당. Tool buttons describe the live `preact-build-console-preview`, preview exposes `role=status`/polite atomic updates, and preview stats use list/listitem semantics. shared Button은 id/aria/data-testid 전달과 HUD pointer guard 이벤트를 보존 | E2E interaction/mobile smoke |
| `src/ui/buildConsoleSnapshot.ts` | BuildConsole snapshot/view-model helper | category/buildableData/current-tab display state 계산, item labels/cost/description/stat metadata, selected tool display payload, localized aria/preview/command labels, selected-tool legacy panel/Preact snapshot 공통 display payload, icon source mapping | BuildConsole 표시용 필터링 결과와 labels를 Preact snapshot과 legacy render가 공유한다. Preact snapshot 발행은 legacy `#ui-overlay`/`#ui-tabs` 존재에 의존하지 않는다. 실제 선택/건설/해금 판정은 기존 `UIManager`/Research/Tutorial 경로가 계속 처리하며, hotkey 배열 계약을 입력으로 받아 보존한다 | E2E build console smoke |
| `src/ui/legacyBuildConsole.ts` | 기존 빌드 콘솔 DOM fallback render/selection helper | `#ui-tabs`, `#ui-overlay`, `#btn-*`, `#btn-remove`, selected tool panel text 생성/갱신, legacy build button active state sync | Preact BuildConsole이 실제 표면이지만 legacy IDs는 E2E/short-landscape fallback 계약이다. 카테고리/툴 클릭은 콜백으로 `UIManager.selectBuilding()`/category request 경로를 재사용하고, 표시 필터링은 `buildConsoleSnapshot.ts` helper를 공유한다. simulation manager를 직접 변경하지 않음 | E2E build/mobile smoke |
| `src/ui/components/SettingsModal.tsx`, `src/ui/shared/ModalOverlay.tsx`, `src/ui/signals/modalState.ts` | Preact 설정 모달과 공통 모달 shell | 기존 설정 모달 open/close, snapshot-localized labels, keyboard-focusable game/audio/graphics/system tabs, tablist/tab/tabpanel linkage, speed/FPS/language segmented groups with pressed state, labeled master volume range, muted/bloom pressed state, audio/bloom/save/load/tutorial reset 요청, overlay click/ESC close, modal dim/blur/slide animation, accessible dialog surface | 실제 설정 적용과 저장/불러오기/튜토리얼 재시작은 EventBus request를 받은 기존 `SettingsUI`/save 경로가 담당하며 `#settings-modal`은 hidden shadow fallback으로 계속 sync된다. ModalOverlay close는 request만 발행하고 legacy canvas focus restore는 기존 close path가 담당. Overlay는 modal contents를 `aria-hidden`으로 가리지 않고 `role=presentation` shell plus stable overlay/panel test IDs를 노출한다. SettingsModal은 visible title/note에 연결된 `role="dialog"`/`aria-modal` surface이며 `preact-settings-title`, `preact-settings-note`, `preact-settings-tabs`, `preact-settings-panel` contracts를 유지한다. Preact tab IDs only connect the visible tabpanel and do not replace legacy settings IDs. animation은 `prefers-reduced-motion`을 존중 | E2E settings smoke |
| `src/ui/settingsDisplay.ts` | Settings modal snapshot/input display helper | SettingsUI current state를 legacy settings input/open state와 localized labels 포함 `SETTINGS_MODAL_UPDATED`용 Preact snapshot 공통 display payload로 변환하고, FPS/volume UI 요청값을 표시 범위로 정규화하며, open-only snapshot patch helper를 제공 | Snapshot과 legacy input/open shadow state는 legacy input DOM 값을 source로 삼지 않고 SettingsUI가 관리하는 current volume/muted/bloom/FPS/speed/language/open state와 i18n labels에서 생성한다. FPS는 `30..240`, volume은 `0..100` 표시 범위로 normalize하지만 실제 설정 적용, 저장/불러오기, 튜토리얼 재시작은 하지 않음 | E2E settings smoke |
| `src/ui/legacySettings.ts` | 기존 settings DOM fallback refs/state helper | `#btn-settings`, `#settings-modal`, audio/bloom/language/speed/FPS controls 조회, pointer guard, open shadow state, legacy input/button active sync | Preact SettingsModal이 실제 표면이고 이 파일은 legacy settings IDs의 hidden shadow fallback만 관리한다. 실제 설정 적용, 저장/불러오기, 튜토리얼 재시작, snapshot 발행은 `SettingsUI`가 유지 | E2E settings/focus/language/save smoke |
| `src/ui/components/TrainingLabModal.tsx` | Preact Neural Operations Lab side panel | 기존 `TrainingLabUI`가 렌더한 Lab overview/planner/jobs/duration 표시, DEFENSE/SYSTEM 탭 request, close request, keyboard-focusable auto toggle request with pressed state, job select request, grouped defense reward request controls with pressed state, tablist/tab/tabpanel linkage, row-labeled data/work progress bars, active/complete/training/locked row tone, stable Preact Lab test IDs | 실제 target/job/reward/auto 조작은 EventBus request를 받은 기존 `TrainingLabUI`와 `ModelTrainingLab` 경로가 담당. Preact row metadata와 visible labels는 snapshot 표시/요청용이며 training planner 계산을 직접 수행하지 않음. Side panel은 non-modal `role="dialog"` 표면이고 visible title을 `aria-labelledby`로 연결한다. Rows expose title/detail linkage, stable `preact-training-lab-row-*-data-progress`/`work-progress` meters with value text, and `preact-training-lab-*` controls used by E2E to exercise auto/reward/job/tab/close paths while legacy `#training-lab-modal` remains synced as a hidden shadow fallback | E2E interaction smoke |
| `src/ui/trainingLabDisplay.ts` | Training Lab display snapshot/view-model helper | Lab GPU overview, planner status/duration, Defense/System job 상태와 localized kicker/close/tabs/auto/reward/progress/tone labels를 legacy shell/row view-model 및 Preact snapshot으로 조립하고, legacy shell/Preact `TRAINING_LAB_UPDATED` snapshot 공통 display payload로 변환하며, open-only snapshot patch helper를 제공 | Snapshot은 legacy `#training-lab-modal` DOM을 파싱하지 않고 `TrainingLabUI`가 전달한 Lab/Planner/Research 상태와 i18n 키에서 직접 생성한다. TrainingPlanner/ModelTrainingLab/ResearchManager 상태 변경은 하지 않음 | E2E Training Lab smoke |
| `src/ui/legacyTrainingLab.ts` | 기존 Training Lab modal fallback lifecycle/render helper | `#training-lab-modal` 생성, open/close shadow state, legacy header/overview/auto/tabs/footer shell render, legacy defense/system row DOM render | 실제 Lab job/auto/reward 변경은 `TrainingLabUI`/`ModelTrainingLab`/planner 경로가 유지한다. 이 파일은 legacy modal compatibility만 담당하고 training 계산이나 Preact snapshot 생성을 직접 수행하지 않음 | E2E Training Lab smoke |
| `src/ui/components/GameOverScreen.tsx` | Preact game-over screen | 기존 `GAME_OVER` 이벤트의 run result summary 표시, localized title/stat/action snapshot labels, core integrity/model progressbar meter with value text, stat card list semantics, keyboard focusable restart/main-menu request 발행, stable Preact action/meter/list test IDs | 기존 `#game-over-screen`과 `#btn-restart`는 hidden shadow fallback으로 유지. Preact action은 EventBus request만 발행하고 `UIManager`가 기존 reload 경로로 처리. Integrity/model meter and stat cards are `GameOverSnapshot` 표시 전용이며 copy, progressbar value text, and stat list entries are rendered from the snapshot. Root is the visible `role="dialog"`/`aria-modal` labeled dialog surface | E2E game-over smoke |
| `src/ui/gameOverDisplay.ts` | Game-over display helper | `RunResultSummary`를 localized GameOver title/failure/stat/model/action labels가 포함된 Preact `GameOverSnapshot`과 legacy stat line 배열을 포함한 공통 display payload로 변환 | run result summary 계산 결과와 i18n copy를 표시 payload로만 바꾼다. Scene/manager 상태를 읽지 않고 game-over 산식이나 reload/request 동작은 변경하지 않음 | E2E game-over smoke |
| `src/ui/legacyGameOver.ts` | 기존 game-over DOM fallback helper | `#game-over-screen` open shadow state, `#btn-restart` reload handler binding, `#game-over-stats` mirror text render | Run summary 계산과 `GAME_OVER_UPDATED` snapshot 발행은 `UIManager`가 유지하고, 이 파일은 legacy DOM 반영만 담당한다. 기존 IDs는 E2E compatibility contract라 유지 | E2E game-over smoke |
| `src/ui/components/WaveResultCard.tsx` | Preact wave result toast | 기존 wave summary snapshot 표시, localized title/close/integrity/stat/history labels, title-to-status and integrity description linkage, Core integrity progressbar with value text, stat chips list semantics, recent wave result timeline list semantics, keyboard focusable Preact close request, stable close/meter/list/title/history test IDs | 기존 `.wave-result-card` class는 hidden shadow fallback으로 유지. Close는 Preact signal만 닫고 기존 DOM notification lifecycle은 유지. Core integrity/stat chip tone과 timeline은 wave result snapshot 표시 전용이며 copy, progressbar value text, and list entries are rendered from snapshots only. Root is the visible labeled/described status region, and history list is labelled by the visible history label | E2E wave summary smoke |
| `src/ui/waveResultDisplay.ts` | Wave result display helper | `WaveResultSummary`를 localized title/close/integrity/stat/history labels가 포함된 Preact snapshot, legacy card content, activity log text를 포함한 공통 display payload로 변환하고, Preact close용 open-state patch helper를 제공 | WaveManager/summary 계산 결과와 i18n copy를 표시용 구조로만 바꾼다. 웨이브 결과 산식이나 게임 상태는 변경하지 않음 | E2E wave summary smoke |
| `src/ui/components/ActivityLog.tsx` | Preact activity log | `UIManager.logMessage()`의 최근 로그 항목과 localized title/filter/history/empty labels 표시, keyboard focusable history toggle, alert filter/count, filter controls linked to live role log entries, article entry semantics, stable Preact log test IDs | 기존 `#activity-log` DOM과 append/remove lifecycle은 유지하되 legacy `.log-entry`는 hidden shadow fallback으로 sync된다. 기본 5개 표시, Preact history/filter toggle은 snapshot 내 최근 12개만 표시/필터링하며 log source와 lifecycle은 기존 `UIManager.logMessage()`가 담당. Alert filter exposes `aria-pressed`; history toggle exposes `aria-expanded`; both control the Preact role log entry region only. Root는 snapshot label을 쓰는 activity log section이고 entries는 `role="log"`/`aria-live="polite"`/`aria-relevant="additions text"`로, 각 entry는 article로 노출. 모바일/좁은 화면 숨김 | E2E activity log smoke |
| `src/ui/components/Tooltip.tsx` | Preact tooltip | `UIManager.showTooltip()`의 title/content/position/close label 표시, viewport-clamped desktop tooltip, 모바일 bottom-sheet, title/body label-description linkage, keyboard focusable close request, stable Preact tooltip test IDs | 기존 `#tooltip` DOM과 `#mobile-info-sheet`는 hidden shadow fallback으로 유지. 데스크톱 Preact 위치와 close label은 `notificationDisplay.ts` payload가 담당하고 component CSS가 clamp만 담당한다. legacy tooltip layout measurement는 숨은 mirror에만 적용한다. 모바일은 Preact bottom-sheet 형태로 표시하며 close는 기존 `hideTooltip()` 경로로 처리. Root는 `aria-labelledby`/`aria-describedby`가 연결된 tooltip role로 노출 | E2E tooltip smoke |
| `src/ui/notificationDisplay.ts` | Notification display snapshot/helper | tooltip open/closed snapshot, tooltip close label, tooltip cursor offset position, desktop/mobile/closed tooltip display payload, desktop tooltip legacy mirror input, mobile tooltip legacy HTML, close hidden marker, activity log localized labels/rolling snapshot/legacy entry 공통 display payload, activity log message text 조립 | `UIManager`의 tooltip/activity EventBus payload와 로그 표시 문구를 표시 전용 구조로 바꾼다. Legacy DOM append/position/remove/hide는 `legacyNotifications.ts`가 계속 담당하며, game state나 simulation manager를 읽지 않음 | E2E activity/tooltip smoke |
| `src/ui/components/TutorialPanel.tsx` | Preact tutorial panel | `TutorialManager.render()`의 active step/progress/step list 표시, snapshot-localized labels, labeled status surface, role progressbar with value text, role list/listitem step semantics, current step `aria-current`, current objective focus, hidden step hint, detail typewriter 표시, skip request, stable Preact tutorial test IDs | 기존 `#tutorial-panel` DOM과 skip button lifecycle은 hidden shadow fallback으로 유지. Preact panel/typewriter/progressbar/step list는 snapshot 표시 전용이며 넓은 데스크톱에서만 노출하고, Preact skip button은 `TUTORIAL_SKIP_REQUESTED`만 발행해 기존 `TutorialManager.completeAll()` 경로를 재사용한다. Phaser guide highlights는 그대로 유지 | E2E tutorial smoke |
| `src/ui/tutorialPanelDisplay.ts` | Tutorial panel display snapshot helper | active step/progress/step list와 localized labels를 legacy tutorial panel 입력과 `TUTORIAL_PANEL_UPDATED`용 Preact snapshot 공통 display payload로 변환 | Tutorial completion, allowed building locks, Phaser guide highlights, 캠페인 전환은 처리하지 않는다. `TutorialManager`가 계산한 step 상태와 i18n labels를 legacy/Preact 표시 payload로만 바꾼다 | E2E tutorial smoke |
| `src/ui/legacyTutorialPanel.ts` | 기존 tutorial panel DOM fallback render/state helper | `#tutorial-panel` 생성/도킹, show/hide shadow state, legacy header/steps/typewriter render, `#btn-skip-tutorial` binding | Preact TutorialPanel이 실제 표면이고 이 파일은 legacy selector/text compatibility를 담당한다. Tutorial completion, allowed building locks, Phaser guide highlights는 `TutorialManager`가 계속 처리 | E2E tutorial smoke |
| `src/ui/components/MobileActionBar.tsx`, `src/ui/components/MobileBuildSummary.tsx` | Preact mobile action controls/build summary surface | `MobileUIManager`의 selected tool summary/action active state 표시와 request 이벤트, snapshot-localized aria/menu labels, keyboard/touch focusable action buttons, aria-pressed active state, cable menu controls/expanded state/haspopup, cable option `menuitemradio` checked state, 52px touch target, icon+label action layout, labeled/described live summary bottom-sheet, stable Preact mobile test IDs | 기존 `#mobile-action-*`, `#mobile-build-summary`, `#mobile-cable-menu` DOM은 유지. 일반 mobile layout에서는 legacy DOM이 `data-preact-shadow`/`aria-hidden`으로 숨겨진 호환 shadow가 되고 Preact controls가 실제 표면이다. Preact action/cable request는 기존 MobileUIManager handler/selectBuilding 경로로 되돌아간다. Short landscape에서는 Preact layer를 숨기고 legacy DOM fallback을 그대로 조작한다 | E2E mobile action smoke |
| `src/ui/mobileActionDisplay.ts` | Mobile action display snapshot helper | selected tool/action status/cable-menu state를 legacy active map/build summary와 MobileActionBar/MobileBuildSummary snapshot, action item labels, cable options with selected state, aria/menu labels, summary text를 포함한 공통 display payload로 변환 | Snapshot은 legacy `#mobile-cable-menu` class를 읽지 않고 `MobileUIManager` state에서 생성한다. 실제 rotate/remove/cable/placement action 수행은 하지 않음 | E2E mobile action smoke |
| `src/ui/legacyMobileControls.ts` | 기존 mobile DOM fallback render/state helper | `#mobile-action-bar`, `#mobile-action-*`, `#mobile-cable-menu`, `#mobile-info-sheet`, `#mobile-build-summary` 생성, action/cable 버튼 render, active class, cable menu class mirror, shadow state, build summary sync | Preact MobileActionBar/MobileBuildSummary가 일반 mobile 실제 표면이고 이 파일은 legacy selector/fallback 계약만 담당한다. Cable menu open source는 `MobileUIManager` state다. Short landscape에서는 legacy controls가 실제 조작 표면이므로 DOM IDs와 click handlers를 유지해야 함 | E2E mobile action/placement smoke |
| `src/ui/domEnvironment.ts` | DOM overlay 환경 helper | mobile layout media query 생성/판정, body class toggle/clear, short-landscape media query 판정, Phaser canvas focus restore | Scene/manager들이 body/canvas DOM API를 직접 만지지 않도록 얇게 모은 helper. Phaser 렌더링이나 game simulation 상태는 변경하지 않음 | E2E modal/mobile focus smoke |
| `src/ui/legacyHudDom.ts` | 기존 Playwright/manager 호환용 legacy DOM roots와 shell state helper | 빈 `#game-hud-shell`에서 top/right/build shell 생성, body-level settings/game-over/tooltip/activity/notification fallback root 생성, shell shadow sync, gameplay HUD display restore, ESC fallback modal hide, speed button active sync | Preact가 실제 UI 표면이지만 기존 DOM IDs는 compatibility contract다. 템플릿 이동/축소 시 `#hud-score`, `#btn-settings`, `#settings-modal`, `#game-over-screen`, `#tooltip`, `#activity-log`, `#notification-container` 등 E2E/manager selector를 유지해야 함. 상태 helper는 DOM fallback만 만지고 실제 settings/game speed 적용은 기존 manager/EventBus 경로가 담당 | E2E 전반 |
| `src/ui/legacyNotifications.ts` | 기존 notification/log/tooltip DOM fallback lifecycle | wave result legacy card append/remove, legacy desktop tooltip positioning, mobile info-sheet mirror, activity log entry append/remove | Preact 컴포넌트가 실제 표면이고 이 파일은 attached hidden shadow fallback과 legacy mobile tooltip HTML 반영만 담당한다. Mobile tooltip 표시 HTML은 `src/ui/notificationDisplay.ts`가 만든다. EventBus snapshot 발행은 `UIManager`가 계속 담당하므로 여기서 game state를 읽거나 simulation manager를 참조하지 않음 | E2E wave/activity/tooltip smoke |
| `src/visuals/visualTheme.ts` | 캔버스 그래픽 패치 팔레트 | 월드, 건물 카테고리, 아이템, 적, 케이블, 오버레이 색상 | 색상만 바꿔도 UI swatch와 캔버스 의미 색이 어긋날 수 있어 `config.ts`와 함께 확인 | build, E2E visual smoke |
| `src/managers/BuildingManager.ts` | 건물 배치/철거/조회, 비용 차감, 멀티타일 등록, 타입별 index, 제거/파괴 이벤트 분리 | 입력 배치, 저장 복원, 적 공격, 인벤토리, UI count | 멀티타일 건물은 모든 타일 key에 같은 객체 등록. 타입 index는 origin 건물 단위 Set으로 유지. 수동 철거는 `BUILDING_REMOVED`, 전투 파괴는 `BUILDING_DESTROYED`로 유지 | E2E placement, `src/utils/buildingLifecycle.test.ts` |
| `src/buildings/BuildingFactory.ts` | 건물 ID -> 클래스 매핑 | 모든 건물 생성 | 새 건물 추가 시 registry 누락 위험 | `src/config.test.ts` 간접 |
| `src/buildings/BaseBuilding.ts` | 모든 건물 공통 상태, 버퍼, HP, 감염, 코드 기반 렌더 기본. 공통 정적 바디는 타입/크기/색상별 텍스처 캐시로 재사용하고 동적 장식/상태 표시는 별도 graphics에 유지 | 생산, 전투, 저장, UI tooltip, 건물 패널 그래픽 | `takeDamage()`는 전투 파괴 경로로 BuildingManager에 위임. 그래픽 변경 시 배경/카테고리 팔레트와 식별성을 같이 확인. subclass가 `this.graphics.clear()`로 자체 바디를 다시 그릴 수 있음. `shouldUseAnimatedVisuals()`가 대형 공장 장식 tween 생성을 제한 | `src/buildings/BaseBuilding.test.ts` |
| `src/buildings/AbstractProcessor.ts` | 레시피 기반 입력 소비/가공/출력 공통 로직 | Processor, WeightTrainer, NeuralTrainer, Recycler | 연구 속도 배율과 진행바 계산 영향. 대형 공장에서는 장식 tween 대신 정적 visual fallback 사용 | `src/utils/productionSimulation.test.ts` 간접 |
| `src/buildings/Miner.ts` | 자원 타일에서 Silicon/Energy 생산 | 자원 추출, 컨베이어 물류 | 자원 타일 위에 있어야 실제 생산. 대형 공장에서는 장식 tween 대신 정적 visual fallback 사용 | 추정: `productionSimulation`은 Downloader 중심 |
| `src/buildings/DataDownloader.ts` | 전력 기반 RAW_DATA 생산 | 초반 데이터 라인 시작 | `MINING_RATE_MULTIPLIER` 효과를 함께 받음. 대형 공장에서는 장식 tween 대신 정적 visual fallback 사용 | `src/utils/productionSimulation.test.ts` |
| `src/buildings/Processor.ts` | RAW_DATA -> LABELED_DATA | 핵심 데이터 라인 | `CONFIG.RECIPES.LABELLING` 의존 | `src/utils/productionSimulation.test.ts` |
| `src/buildings/WeightTrainer.ts` | LABELED_DATA 2개 -> WEIGHT_UPDATE | 고가치 Lab 작업 재료 | Neural Operations Lab 작업 진행도 +5 입력 | `src/utils/productionSimulation.test.ts` |
| `src/buildings/NeuralTrainer.ts` | MODEL_TRAINING / INFERENCE_UNIT_PRODUCTION 레시피 전환 | 후반 생산, 고급 연구 | 클릭 시 레시피 전환하며 버퍼 초기화 | E2E는 간접 |
| `src/buildings/ModelTrainingLab.ts` | Neural Operations Lab 입력/처리 노드. 데이터 아이템을 전역 `TrainingPlannerManager` active job에 투입하고, Lab/GPU power를 학습 진행에 기여 | 방어 모델 성장, 시스템 프로토콜 해금, 튜토리얼 최종 Lab 단계 | Lab별 active job 저장은 전역 planner로 이전. 방어 모델 진행 상태와 정확도/공격력 보상 선택은 `MainScene.defenseModelStates`, 시스템 프로토콜 진행도는 `ResearchManager.jobProgress`, 자동/수동 작업 선택은 `TrainingPlannerManager`에 저장. 전투에는 `DefenseTower`가 시간 감소를 적용한 실효 정확도를 사용 | `src/managers/TrainingPlannerManager.test.ts`, `src/utils/modelTrainingSummary.test.ts`, `src/utils/modelTrainingProgress.test.ts` |
| `src/buildings/GpuCluster.ts` | Neural Operations Lab 인접 고전력 가속기 | 정확도 100 이후 학습 시간 단축 | Research 해금이 아니라 모델 정확도 기반 UI gating을 사용. 전력이 없으면 가속에 기여하지 않음. 대형 공장에서는 장식 tween 대신 정적 visual fallback 사용 | `src/utils/modelTrainingProgress.test.ts` 간접 |
| `src/buildings/DefenseTower.ts` | Classifier/Filter/Firewall 공격, 명중률, 연구 보정, 모델 정확도/공격력 상태 적용 및 실시간 타겟 추적/배리어/방화벽 요동 등 커스텀 벡터 비주얼 탑재 | 방어 전투, 적 제거 | Classifier/Filter/Firewall 드로잉 및 누수 없는 트윈 클린업 구현. 대형 공장에서는 장식 tween 대신 정적 visual fallback 사용 | E2E 방어 배치 smoke |
| `src/buildings/Core.ts` | Core HP와 수신 데이터 총량 집계 | 게임오버, 런 통계 | 데이터 수신 시 `totalDataReceived`만 증가하며 연구 화폐는 만들지 않음 | E2E, save smoke |
| `src/buildings/Conveyor.ts` | Silicon 물리 물류, 방향 기반 pull/push 및 스크롤 갈매기(Chevron) 벡터 렌더링 | 자원 라인 | 3중 네온 쉐브론 흐름선 및 레일 외곽선 효과, 누수 없는 트윈 적용 (FastLink 공통). 대형 공장에서는 장식 tween 대신 정적 visual fallback 사용 | E2E placement smoke |
| `src/buildings/Storage.ts` | 단일 타입 저장, inputBuffer를 출력으로도 사용 및 실시간 잔여량 게이지 렌더링 | 인벤토리, 케이블/컨베이어 버퍼 | 저장 용량 비율(inputBuffer.length / maxBufferSize)에 연동되어 갱신되는 2중 원형 네온 아크(Arc) 게이지 탑재 | E2E storage placement |
| `src/buildings/DataCache.ts` | 데이터 아이템 전용 Storage | 케이블/AP 데이터 버퍼 | RAW/Labeled/Weight/Trained/Inference 허용 및 용량 맞춤형 원형 네온 아크 게이지 연계 | AP relay 테스트 간접 |
| `src/buildings/Unloader.ts` | Storage 뒤쪽에서 아이템 추출 및 진행 방향 피스톤 피드백 렌더링 | 저장고 -> 라인 연결 | 진행 방향 피스톤 피드백 렌더링 및 누수 없는 트윈 탑재. 대형 공장에서는 장식 tween 대신 정적 visual fallback 사용 | E2E early gating |
| `src/buildings/PowerPlant.ts` | Energy 자원 위 발전소 활성 여부 및 융합 제어 코어 렌더링 | 전력 생산 | 액티브/인액티브 드로잉과 누수 없는 트윈 탑재. 대형 공장에서는 장식 tween 대신 정적 visual fallback 사용 | PowerManager 간접 |
| `src/buildings/PowerNode.ts`, `SolarPanel.ts` | 전력 범위 노드 및 솔라 표면 네온 그리드/반사광 연출 | 전력망 확장 | PowerNode/SolarPanel 비주얼 탑재. 대형 공장에서는 장식 tween 대신 정적 visual fallback 사용 | `src/utils/powerPreview.test.ts` |
| `src/buildings/AccessPoint.ts` | AP 시각/기본 범위/대역폭 상태 | 무선 데이터 릴레이 | 전송 정책은 `CableManager`와 `utils/apRelay.ts`. 대형 공장에서는 장식 tween 대신 정적 visual fallback 사용 | `src/utils/apRelay.test.ts` |
| `src/buildings/Repeater.ts` | 무버퍼 유선 케이블 중계 노드 | 긴/우회 케이블망, 전력 의존 데이터 경유 | `CableManager`가 powered Repeater를 투명 relay endpoint로 처리. Repeater 파괴/제거 시 연결 케이블도 제거 | `src/managers/CableManager.test.ts` |
| `src/managers/TickSystem.ts` | 고정 틱 실행, dirty 전력 갱신, 케이블/건물 onTick 순서 | 생산, 전력, AP, 케이블 | `gameSpeed`가 tick interval을 나눔. 전력망은 full tick마다 조건부 `updateIfDirty()`만 호출 | 생산/파워 관련 테스트 간접 |
| `src/managers/CableManager.ts` | 케이블 연결, 거리 비용/환불, 최대 길이, BLOCKER 충돌, 큐, 데이터 이동, Repeater 경유, AP 자동 릴레이, 케이블/패킷 펄스 렌더 | 데이터 물류, 저장 복원 | `canConnect()`가 UI preview와 실제 연결의 단일 검증 경로. Repeater는 무버퍼라 relay 실패 시 incoming cable queue에 packet이 남음. AP 자동 릴레이는 타입 index와 tile bucket cache로 주변 후보만 조회. pulse/AP wave는 active 상한과 object pool로 생성량/GC를 제한 | `src/managers/CableManager.test.ts`, `src/utils/cablePath.test.ts`, `src/utils/apRelay.test.ts`, E2E cable |
| `src/managers/EffectsManager.ts` | 빌드/피해/웨이브/경고/락온/훈련 이펙트와 경고 마커 | 시각 피드백, 전력/버퍼 경고, 적/방어 효과 | 전력/버퍼 경고는 BuildingManager 타입 index와 기존 marker set을 조합해 후보만 갱신. 이펙트 생성량 변경 시 pooling/cleanup 확인 | `src/managers/EffectsManager.test.ts` |
| `src/managers/PowerManager.ts` | 전력 노드 네트워크 구성, 풋프린트 중심 범위 계산, 소비자 할당, blackout 적용, dirty rebuild 제어, node spatial overlap 탐색 | 생산/방어/케이블 활성 조건 | `hasPower` 적용 순서와 멀티타일 건물 중심 범위가 전체 런타임에 영향. 안정 상태에서는 `TickSystem`이 `updateIfDirty()`로 rebuild를 건너뜀. rebuild 시에도 전력 관련 타입 index와 spatial bucket 후보 비교를 사용 | `src/managers/PowerManager.test.ts`, `src/utils/powerPreview.test.ts`, `src/utils/geometry.test.ts` 보조 |
| `src/managers/WaveManager.ts` | 웨이브 타이머, 적 스폰, 코어 중심 target 전달, 보상, boss aura, next-wave briefing 발행, 적 spatial range query | 방어 압박, 연구 개방 신호, 튜토리얼 mock wave, 타워 타겟 검색 | 튜토리얼 중 FIRST_WAVE 이전에는 웨이브를 동결하고, FIRST_WAVE 단계에서 북쪽 gate mock wave를 사용. 웨이브 계산은 `utils/waveSimulation.ts`와 분리되어 있음. `WAVE_UPDATE`는 남은 초가 바뀔 때만 발행해 DOM 갱신 이벤트를 줄임. 타워 range query는 적 spatial bucket을 사용 | `src/utils/waveSimulation.test.ts`, `src/utils/waveBriefingKey.test.ts`, `src/utils/gridPath.test.ts`, E2E threat panel |
| `src/enemies/BaseEnemy.ts` | 적 HP/이동/pathfinding/건물 공격/특수 효과/적 실루엣 렌더 | 코어 피해, 건물 파괴, 감염, 보스 오라 | pathfinding 방문 제한과 blocking 규칙 변경 주의. 경로 계산은 `utils/gridPath.ts`로 분리. 런타임은 start/target tile path cache와 id 기반 initial stagger로 A* spike를 줄이고, 적 상태 tween은 active 상한을 사용 | `src/utils/gridPath.test.ts`, `src/utils/enemyBuildingInteraction.test.ts` |
| `src/managers/MapManager.ts` | organic 자원 패치와 BLOCKER boundary/cluster 지형 생성, enemy lane 예약/BFS 복구, map/build/camera bounds 조회, 작은 튜토리얼 arena 맵, 캠페인 seed 맵 | 초기 맵, 튜토리얼 맵, 저장 복원, 적 경로 차단, 배치/카메라 제한 | standard preset은 `-64..64` world/build bounds와 `-60..60` resource bounds를 가진 큰 유한 맵. `RESOURCE_RINGS`가 있으면 중반 거리대에 랜덤 자원을 집중하고, standard 자원은 dense center/irregular edge blob으로 배치. `earlyLaneBlockers`는 먼저 outer boundary terrain pass로 외곽을 더 조밀하게 만들고, 예약된 적 통로를 피하는 blob/rough-line/cluster BLOCKER 군집을 만든 뒤 작은 파편 cleanup과 BFS로 주요 route 경로를 보장. bounds 필드는 `MainScene.isBlocked()`와 `CameraController`가 사용 | `src/managers/MapManager.test.ts` |
| `src/managers/GridRenderer.ts` | 배경, 섹터 그리드, 자원 패치, BLOCKER 지형 렌더. 보이는 월드 영역은 `CONFIG.OPTIMIZATION.GRID_CHUNK_TILES` 단위 텍스처 청크로 캐시 | 카메라 이동/줌 중 월드 시각화 | forced redraw는 캐시를 비워 map/load stale texture를 방지. 일반 pan은 기존 청크 이미지를 재사용하고, zoom detail tier가 바뀌면 별도 청크 키를 사용 | `src/managers/GridRenderer.test.ts`, build, E2E startup |
| `src/managers/SaveManager.ts` | localStorage 저장/로드, 런타임 재구성, dirty autosave gate, deferred chunk autosave | 자동 저장, 설정, 연구, 케이블/적/건물/맵 복원 | 포맷 변경 시 migration과 SaveData 갱신. 튜토리얼 모드에서는 일반 캠페인 저장 슬롯을 쓰지 않고, 캠페인에서는 `settings.mapType`과 resource/terrain map 데이터를 복원. 변경 없는 안정 상태에서는 autosave가 전체 JSON 생성을 건너뛰고, 필요한 autosave는 buildings/items/cables/enemies/resource/terrain snapshot 수집을 여러 macrotask로 나눔 | `src/utils/saveMigration.test.ts`, `src/utils/enemyRestore.ts`, E2E save, `tests/e2e/performance.spec.ts` |
| `src/utils/saveMigration.ts` | 구버전 저장 데이터 기본값 보정 | 로드 안정성 | 새 필드는 기본값과 버전 처리 필요 | `src/utils/saveMigration.test.ts` |
| `src/managers/ResearchManager.ts` | 시스템 프로토콜 작업 진행도, 완료 상태, 효과 누적 | Neural Operations Lab, 건물 해금, 밸런스 보정 | Lab에서 데이터 아이템 가치만큼 진행도를 올리며 multiplier 효과는 곱하고 bonus는 더함. timed training은 `TrainingPlannerManager`가 Lab/GPU power amount를 넘겨 진행할 수 있음 | `src/managers/ResearchManager.test.ts` |
| `src/managers/TrainingPlannerManager.ts` | 전역 자동 학습 planner. Lab 간 active job 공유, 자동/수동 모드, 방어 모델/시스템 프로토콜 후보 점수, Lab/GPU power 합산 진행 처리 | Neural Operations Lab 자동화, 방어 모델 성장, 시스템 프로토콜 해금 | AUTO_DECIDE에서만 선택을 덮어쓰며, 학습 중 또는 90% 이상 준비된 작업은 유지. pressure가 낮을 때 시스템 프로토콜을 우선하고, pressure가 높거나 실효 정확도가 낮으면 방어 모델 정확도/공격력 후보를 선택 | `src/managers/TrainingPlannerManager.test.ts` |
| `src/managers/PerformanceStats.ts` | frame sample, p95/long frame, entity count, 최적화 counter 수집 | 성능 fixture, 수동 디버깅, 회귀 비교 | `window.__GRADIUM_PERF__`와 `scene.performanceStats.getSummary()`로 조회. `autosaveChunks`는 autosave snapshot 분산 수집 검증에 사용. counter 이름 변경 시 Playwright perf fixture와 문서 갱신 | `tests/e2e/performance.spec.ts` |
| `src/managers/UIManager.ts` | legacy DOM 보장 호출, HUD/tactical/build snapshot 발행, 툴팁/로그 snapshot 발행, 모달 위임, Preact HUD/RightRail/BuildConsole/GameOver/WaveResult/ActivityLog/Tooltip용 request 처리, settings open sync | 모든 DOM UI, Preact HUD bridge | `ensureLegacyHudShell()`은 `src/ui/legacyHudDom.ts`의 생성 헬퍼만 호출하고, legacy shell shadow/modal/speed button DOM sync도 `src/ui/legacyHudDom.ts`에 위임한다. Top HUD resource/power/wave-start 공통 display payload 조립은 `src/ui/topHudDisplay.ts`, Top HUD legacy stat mirror는 `src/ui/legacyTopHud.ts`, tactical labels/objective/wave/power/defense 표시 payload와 RightRail common display payload 조립은 `src/ui/tacticalPanelDisplay.ts`, tactical legacy DOM refs/update는 `src/ui/legacyTacticalPanels.ts`, legacy build console DOM render/selection sync는 `src/ui/legacyBuildConsole.ts`, BuildConsole labels/snapshot/selected-tool legacy panel 공통 display payload 조립은 `src/ui/buildConsoleSnapshot.ts`, wave result snapshot/card/log 공통 display payload 조립은 `src/ui/waveResultDisplay.ts`, game-over snapshot/stat line 공통 display payload 조립은 `src/ui/gameOverDisplay.ts`, tooltip open/close/activity snapshot, desktop/mobile tooltip legacy mirror input, activity log legacy entry, 로그 표시 문구 조립은 `src/ui/notificationDisplay.ts`, game-over legacy open/restart/stats DOM 반영은 `src/ui/legacyGameOver.ts`, wave/log/tooltip DOM fallback append/position/remove/hide는 `src/ui/legacyNotifications.ts`, mobile layout/canvas focus 환경 판정은 `src/ui/domEnvironment.ts`에 위임한다. DOM id/text는 E2E와 연결. 빌드 버튼 id는 유지해야 hotkey/E2E 회귀가 적음. 데스크톱 legacy `#top-hud`/`#hud-right-rail`/`#bottom-ui-container`/`#build-console`은 `data-preact-shadow`/`aria-hidden`으로 숨겨 Preact TopBar/RightRail/BuildConsole이 실제 표면이다. Mobile portrait에서도 bottom/build legacy는 shadow이고 Preact BuildConsole이 실제 표면이며, short landscape에서는 legacy compact fallback을 유지한다. RightRail snapshot은 legacy DOM panel text를 다시 읽지 않고 표시 payload와 `WaveBriefing` route/threat/special 메타로 발행. Preact BuildConsole 요청도 기존 `selectBuilding()`/`createBuildingButtons()` 경로로 처리. Game-over run summary 계산은 `UIManager`가 유지하고, Preact action과 legacy restart는 reload 경로를 공유한다. Wave result `.wave-result-card`, activity `.log-entry`, `#tooltip`, `#mobile-info-sheet`도 hidden shadow fallback으로 sync되고 Preact card/log/tooltip이 실제 표면이다. Preact activity snapshot은 최근 12개 보관 | E2E 전반 |
| `src/managers/SettingsUI.ts` | 설정 적용, 저장/로드/언어/속도/FPS/튜토리얼 재시작 request 처리, Preact settings snapshot 발행 | 설정 UI, Preact modal bridge | 닫기 후 canvas focus 복원 기대. 저장된 FPS는 `src/ui/settingsDisplay.ts`의 normalize helper로 `30..240`에 맞추고 기본값은 `main.ts`와 같은 60 FPS로 유지. Volume/muted/bloom/FPS/open 여부는 SettingsUI current state가 source이고 legacy input/modal DOM은 mirror/fallback이다. Legacy settings DOM refs/guard는 `src/ui/legacySettings.ts`에 위임하고, localized labels, legacy open/input + Preact snapshot 공통 display payload 변환과 FPS/volume input normalization은 `src/ui/settingsDisplay.ts`에 위임한다. Preact TopBar settings shortcut은 기존 `openModal()` 경로를 재사용하고, open 중 legacy `#settings-modal`은 `data-preact-shadow`/`aria-hidden`으로 숨겨 Preact dialog가 실제 조작 표면이 된다. Preact tutorial reset request는 legacy reset 버튼과 같은 `MainScene` tutorial restart 경로를 사용 | E2E focus/language/save |
| `src/managers/TrainingLabUI.ts` | Neural Operations Lab Preact/legacy request coordinator | AUTO/MANUAL planner 상태, 방어 모델/시스템 프로토콜 작업 선택, 정확도/공격력 보상 모드, 진행도, 학습/GPU 상태 표시 요청 처리 | Preact request와 legacy row callbacks는 같은 private helper를 거쳐 `ModelTrainingLab`/planner 경로를 재사용한다. Legacy `#training-lab-modal` 생성/open shadow/shell render/row DOM render는 `src/ui/legacyTrainingLab.ts`에 위임하고, Lab overview/planner/duration, Defense/System row view-model 및 legacy shell/Preact snapshot 공통 display payload 변환은 `src/ui/trainingLabDisplay.ts`에 위임한다. Snapshot row 계산/발행은 legacy modal element 존재 여부에 의존하지 않고, legacy row render는 fallback mirror로만 실행된다. close request는 기존 canvas focus restore 경로를 재사용 | E2E 간접 |
| `src/managers/MobileUIManager.ts` | 모바일 action/cable request 처리, Preact MobileActionBar snapshot 발행, legacy mobile fallback sync 호출 | 모바일 조작 | selected tool summary/action active map/action labels/cable options/aria labels는 `src/ui/mobileActionDisplay.ts`의 common display payload에 위임한다. `#mobile-action-*`, `#mobile-build-summary`, `#mobile-cable-menu` legacy DOM 생성/active/shadow/menu/summary sync는 `src/ui/legacyMobileControls.ts`에 위임하고, mobile/short-landscape 판정은 `src/ui/domEnvironment.ts`를 사용한다. Cable menu open 여부는 manager state가 source이며 `src/ui/mobileActionDisplay.ts`가 snapshot payload로 변환한다. 일반 mobile layout에서는 legacy DOM에 shadow attrs를 붙이고 Preact 표면이 클릭을 받으며, short landscape에서는 Preact CSS가 숨겨지므로 legacy fallback을 계속 사용한다. Preact cable option도 같은 `selectBuilding()` 경로로 처리 | E2E mobile |
| `src/managers/TutorialManager.ts` | 단계별 건물 잠금, 튜토리얼 완료 조건 검사, 데이터 기반 월드 고스트/흐름/범위 힌트 Phaser 렌더링, Preact TutorialPanel snapshot/skip bridge | 건물 역할 온보딩, 빌드 버튼 갱신, WaveManager mock wave 연동, 튜토리얼 완료 시 새 캠페인 전환 | 완료 조건은 `tutorialFlow.completion` 메타를 따름. 자동 진행, 특정 건물 생산, 케이블 연결, 전력 온라인, `WAVE_ENDED`, `MODEL_TRAINING_TARGET_SET`을 모두 처리. 힌트 렌더는 `tutorialFlow.ts`가 단일 원천. Legacy panel 입력과 localized labels 포함 Preact snapshot 공통 display payload 변환은 `src/ui/tutorialPanelDisplay.ts`에 위임하고, 기존 `#tutorial-panel` DOM 생성/render/typewriter/skip/shadow sync는 `src/ui/legacyTutorialPanel.ts`에 위임한다. Preact skip request도 같은 `completeAll({ transitionToCampaign: true })` 경로를 사용 | `src/utils/tutorialFlow.test.ts`, `tests/e2e/tutorial-guidance.spec.ts` |
| `src/controllers/InputController.ts` | 캔버스 입력, 배치/케이블/철거/툴팁/모바일 탭, cursor dirty cache | 실 조작의 중심 | DOM UI guard 셀렉터는 실제 남아 있는 legacy/Preact overlay roots와 맞춰 유지. 좌표 snap, unlock 체크 주의. ghost/tooltip은 signature/throttle 기반으로 갱신하므로 상태 변경 이벤트에서 cache invalidation 유지 | E2E interaction |
| `src/controllers/OverlayController.ts` | 방어 범위/전력망 오버레이 그리기 | F1/F2, 모바일 토글 | 연구 보너스 반영 | E2E overlay |
| `src/managers/EventBus.ts` | typed pub/sub와 owner cleanup | 매니저 간 결합 완화 | owner 이름 누락 시 shutdown 누수 가능 | `src/managers/EventBus.test.ts` |
| `src/utils/waveSimulation.ts` | 웨이브 수량/HP/경로/브리핑 순수 계산 | WaveManager, UI threat panel, tests | 난이도/경로 정책의 기준 파일 | `src/utils/waveSimulation.test.ts` |
| `src/utils/gridPath.ts` | 적 경로 A* 순수 계산 | BaseEnemy 이동/pathfinding | no-path는 빈 배열로 유지해 런타임이 blocked fallback을 결정. 마지막 점은 정확한 target world 좌표를 보존 | `src/utils/gridPath.test.ts` |
| `src/utils/geometry.ts` | 멀티타일 건물 footprint center와 테두리 기반 범위 tile 계산 | WaveManager target, EffectsManager route guide, PowerManager coverage | grid size와 건물 WIDTH/HEIGHT 변경 시 중심/테두리 좌표 기대값 확인 | `src/utils/geometry.test.ts` |
| `src/utils/buildingLifecycle.ts` | 건물 수동 제거/전투 파괴 이벤트 매핑 | BuildingManager lifecycle events | 이벤트 의미가 웨이브 통계와 피드백에 연결됨 | `src/utils/buildingLifecycle.test.ts` |
| `src/utils/enemyRestore.ts` | 저장된 적 HP/maxHP 복원 계산 | SaveManager active wave load | difficulty multiplier 누락 방지 | `src/utils/saveMigration.test.ts` |
| `src/utils/waveBriefingKey.ts` | next-wave briefing 중복 발행 방지 key | WaveManager -> UIManager briefing | countdown-only 변경은 `WAVE_UPDATE`로 처리 | `src/utils/waveBriefingKey.test.ts` |
| `src/utils/progressionGates.ts` | 초반 목표와 고급 시스템 숨김 정책 | UI 빌드바/목표 패널 | 초반 AP/Fast/Fiber/Unloader 노출 정책 | `src/utils/progressionGates.test.ts` |
| `src/utils/productionSimulation.ts` | 생산 라인 장기 시뮬레이션 | 밸런스 검증용 순수 모델 | 실제 CableManager와 완전 동일하지 않을 수 있음 | `src/utils/productionSimulation.test.ts` |
| `src/utils/apRelay.ts` | AP 소스/타겟 선택 정책 | CableManager 무선 릴레이 | Storage/DataCache를 자동 source에서 제외 | `src/utils/apRelay.test.ts` |
| `src/utils/cablePath.ts` | 자유각 케이블 거리와 타일 교차 샘플링 | 케이블 비용/최대 길이/BLOCKER 충돌 | 샘플링 판정과 화면에 보이는 선이 어긋나면 케이블 UX가 나빠짐 | `src/utils/cablePath.test.ts` |
| `src/utils/enemyBuildingInteraction.ts` | 적의 건물 공격 우선순위 | BaseEnemy 공격 타겟 | Core/Firewall/일반 건물 우선순위 확인 | `src/utils/enemyBuildingInteraction.test.ts` |
| `src/utils/tutorialFlow.ts` | 건물 역할 튜토리얼 단계/진행/허용 건물/완료 메타/시각 힌트 정의 | TutorialManager, UI copy, 월드 고스트/흐름 힌트 | i18n 키와 단계 순서 연결. `completion` 메타가 실제 진행 조건이므로 새 단계 추가 시 TutorialManager 검사와 E2E를 함께 갱신. 좌표는 튜토리얼 맵의 자원/전력/빈 칸 조건과 함께 유지 | `src/utils/tutorialFlow.test.ts`, `tests/e2e/tutorial-guidance.spec.ts` |
| `tests/e2e/app-smoke.spec.ts` | 실제 브라우저 smoke, 배치/케이블/모바일/저장/언어, Preact HUD 표면 검증 | 회귀 최종 방어선 | 시작은 Preact `#preact-main-menu-start`를 우선 클릭하고 기존 canvas 좌표 클릭을 fallback으로 유지. TopBar/RightRail/BuildConsole/TutorialPanel/Settings/WaveResult/GameOver/ActivityLog/Tooltip/MobileActionBar/TrainingLabModal는 `preact-*` test id도 확인하고, 설정 open/close/language/speed/save와 Training Lab auto/reward/job/tab/close는 Preact request path를 직접 클릭하면서 legacy `#settings-modal`/`#training-lab-modal`이 hidden shadow fallback으로 sync되는지 확인한다. Settings tab navigation uses the Preact tab role contract. Wave summary smoke verifies Preact card plus legacy `.wave-result-card` hidden shadow sync. Game-over smoke emits existing `GAME_OVER` and verifies Preact dialog plus legacy `#game-over-screen` hidden shadow sync. Activity log smoke verifies visible `preact-activity-log` plus legacy `.log-entry` hidden shadow sync. Tooltip smoke calls `showTooltip()`, verifies visible `preact-tooltip`, close request, and legacy `#tooltip` hidden shadow sync. Desktop build category/tool selection, portrait/mobile action 일부는 Preact request path를 직접 클릭한다. Preact BuildConsole이 숨겨지는 mobile/compact viewport에서는 legacy build control fallback을 사용한다. Short landscape에서는 Preact mobile action mirror가 숨겨지는 계약을 확인하고 legacy action fallback을 클릭한다. 배치 좌표는 시작 Core/Storage footprint와 겹치지 않는 타일만 클릭해야 함 | Playwright |
| `tests/e2e/tutorial-guidance.spec.ts` | 실제 브라우저 튜토리얼 힌트/완료 smoke, Preact tutorial panel startup smoke | 튜토리얼 고스트 좌표, 리소스 정합성, 모델 훈련소 가중치 입력, 완료 후 새 캠페인 전환 | 시작은 Preact start ID 우선, canvas 좌표 fallback. Preact TopBar는 모든 viewport에서 확인하고, TutorialPanel은 desktop visible/mobile attached 상태를 확인하되 튜토리얼 완료 판단은 `TutorialManager`/world state가 권위. `tutorialFlow.ts` 좌표와 `WaveManager` 튜토리얼 완료 이벤트 변경 시 함께 갱신. 캠페인 전환 검증은 Core footprint가 아닌 튜토리얼 전용 건물 좌표로 확인 | Playwright |
| `tests/e2e/performance.spec.ts` | desktop Chromium에서 100/500/1000 건물 perf fixture 생성, `PerformanceStats` summary, 1000 건물 autosave chunk 검증, Preact menu/topbar startup smoke | 대형 공장 성능 회귀 기준선 | 시작은 Preact start ID 우선, canvas 좌표 fallback. 직접 scene API로 STORAGE를 대량 배치한다. threshold 강화 시 환경별 변동성을 고려. autosave 검증은 campaign mode로 고정해야 저장이 활성화됨 | Playwright desktop |
| `index.html` | Phaser mount, `#preact-hud` overlay root, 빈 `#game-hud-shell` legacy container | `src/ui/legacyHudDom.ts`가 legacy HUD IDs와 settings/game-over/tooltip/activity/notification fallback roots를 런타임 생성하고, Preact HUD가 실제 overlay를 담당 | 정적 top/right/build/settings/game-over/tooltip/activity/notification markup은 제거됨. 기존 HUD/패널/빌드/모달 id 변경은 `legacyHudDom`과 E2E 영향. `#preact-hud`는 Phaser canvas sibling root | E2E 전반 |
