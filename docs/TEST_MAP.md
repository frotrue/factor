# 테스트 맵

자세한 실행 팁은 기존 [AUTOMATED_TESTING_GUIDE.md](./AUTOMATED_TESTING_GUIDE.md)도 함께 참고하세요. 이 문서는 다음 AI 세션이 “어떤 변경에 어떤 테스트를 봐야 하는지” 빠르게 고르기 위한 지도입니다.

## 현재 테스트 도구

- Vitest: 순수 함수, config 계약, manager/building 일부 단위 테스트
- Playwright: 실제 브라우저에서 Phaser canvas + DOM UI smoke 테스트
- Vite build: 타입/번들 검증 성격

## 테스트 실행 명령어

```powershell
npm test
npm run build
npm run test:e2e
npx playwright test --workers=1
node scripts\capture-ui-baseline.cjs
```

로컬 안정 기준은 `npx playwright test --workers=1`입니다. Playwright 브라우저가 없으면 `npx playwright install chromium`을 실행합니다.
UI 재개편 전 기준선 스크린샷과 Phase 0 검증 결과는 [UI_PHASE_0_BASELINE.md](./UI_PHASE_0_BASELINE.md)에 기록합니다.

## 테스트 구분

| 구분 | 위치 | 설정 | 용도 |
|---|---|---|---|
| 유닛/순수 로직 | `src/**/*.test.ts` | `vitest.config.ts` | config, i18n, utils, 일부 manager/building 검증 |
| E2E/smoke | `tests/e2e/*.spec.ts` | `playwright.config.ts` | 실제 브라우저 조작, DOM UI, PC HUD shell, 모바일/데스크톱 smoke, 튜토리얼 완료 흐름 |
| 빌드 | 전체 소스 | `vite.config.ts`, `tsconfig.json` | 번들/타입/에셋 참조 검증 |

## 테스트 파일 위치와 검증 대상

| 테스트 파일 | 검증 기능 |
|---|---|
| `src/config.test.ts` | config 참조, 건물 텍스처, Core footprint, 건물 HP, BLOCKER 지형 |
| `src/i18n.test.ts` | 기본 언어, 번역 fallback/key 동작 |
| `src/buildings/BaseBuilding.test.ts` | 공통 건물 상태와 기본 동작, 기본 그래픽 fallback, 정적 바디 텍스처 캐시 재사용 |
| `src/managers/EventBus.test.ts` | owner/callback 기반 이벤트 제거 |
| `src/managers/CableManager.test.ts` | 케이블 거리 비용, 최대 길이, 연구 길이 보너스, BLOCKER 충돌, `costPaid` 저장 |
| `src/managers/EffectsManager.test.ts` | 경고 마커 등 이펙트 manager 안정성 |
| `src/managers/GridRenderer.test.ts` | 그리드 청크 텍스처 캐시 재사용, forced redraw 시 stale 청크/텍스처 정리 |
| `src/managers/MapManager.test.ts` | 지형 blocker, seed 기반 캠페인 맵 재현, standard enemy route 경로 보장과 reserved lane 무장애물 검증, 불규칙 outer boundary terrain density, 긴 직선형 blocker 억제, 작은 blocker cluster cleanup, organic resource edge, starter 자원 보장, 캠페인 RESOURCE_RINGS 중반 자원 집중, 작은 튜토리얼 arena 맵, 튜토리얼/캠페인 wrapper 경로 분리. Tutorial/campaign 분리 검증은 random flake를 피하려고 fixed seed standard map을 사용 |
| `src/managers/PowerManager.test.ts` | dirty 상태에서만 전력망 rebuild가 실행되고 안정 tick에서는 skip되는지 검증 |
| `src/managers/ResearchManager.test.ts` | Lab 기반 시스템 프로토콜 진행도, 완료, 선행조건, 저장 복원 |
| `src/managers/TrainingPlannerManager.test.ts` | 자동 학습 planner의 90% hold, high-pressure 방어 정확도 선택, low-threat 시스템 프로토콜 선택, Lab/GPU power 변환 |
| `src/utils/apRelay.test.ts` | AP 자동 릴레이 source/target 선택 |
| `src/utils/cablePath.test.ts` | 자유각 케이블 거리 계산과 선분 tile 샘플링 |
| `src/utils/buildingLifecycle.test.ts` | 수동 제거와 전투 파괴 이벤트 의미 분리 |
| `src/utils/enemyBuildingInteraction.test.ts` | 적의 건물 공격 우선순위 |
| `src/utils/geometry.test.ts` | 멀티타일 건물 중심 좌표와 중심 기반 범위 tile 계산 |
| `src/utils/gridPath.test.ts` | 적 A* 경로 계산, blocker no-path, 선택적 8방향/strict diagonal corner rule, 초기 blocker 지형에서 Core center 도달 |
| `src/utils/powerPreview.test.ts` | 전력 범위 helper |
| `src/utils/productionSimulation.test.ts` | 장기 생산 라인 시뮬레이션 |
| `src/utils/saveMigration.test.ts` | 저장 데이터 기본값, HP/terrain/settings/buffer/mapType 보정, 적 HP 복원 clamp |
| `src/utils/tutorialFlow.test.ts` | 건물 역할 튜토리얼 단계/진행, 완료 메타, 시각 힌트 데이터, FIRST_WAVE/모델 학습 최종 단계 |
| `src/utils/waveSimulation.test.ts` | 웨이브 수량, 난이도, DDoS/boss, 경로, 브리핑 |
| `src/utils/waveBriefingKey.test.ts` | WaveManager briefing 중복 발행 방지 key |
| `src/utils/waveResultSummary.test.ts` | 웨이브 결과 요약 계산/문구 |
| `src/utils/progressionGates.test.ts` | 초반 목표 순서, 고급 시스템 gating |
| `src/utils/modelTrainingSummary.test.ts` | 모델 훈련 정확도/공격력/데이터/진행 요약 |
| `src/utils/modelTrainingProgress.test.ts` | 학습 데이터 가치, 요구량 1.3배 스케일, 소모 데이터량 기반 학습 시간, 선택된 정확도/공격력 보상, GPU 가속 계산 |
| `src/utils/runResultSummary.test.ts` | 게임오버/런 결과 요약 |
| `src/ui/buildConsoleSnapshot.test.ts` | BuildConsole category/tool display state가 legacy DOM 없이 생성되고 research/GPU lock 필터, selected-tool payload, shared item selected/hotkey/remove metadata를 유지하는지 검증 |
| `src/ui/settingsDisplay.test.ts` | Settings display helper가 FPS/volume 값을 표시 경계에서 정규화하고 legacy input mirror와 Preact snapshot에 같은 값을 내보내는지 검증 |
| `src/ui/trainingLabDisplay.test.ts` | Training Lab display helper가 Defense/System row progress를 finite 0-100 표시 값으로 정규화하고 legacy row와 Preact snapshot progress를 맞추는지 검증 |
| `src/ui/mobileActionDisplay.test.ts` | Mobile action display helper가 cable/transient tool 판정, legacy active map, Preact action/cable option snapshot, build summary mirror를 같은 source로 유지하는지 검증 |
| `src/ui/notificationDisplay.test.ts` | Notification display helper가 tooltip line normalization/desktop offset/mobile legacy content와 activity log cap을 Preact snapshot 및 legacy mirror payload에 같이 적용하는지 검증 |
| `src/ui/waveResultDisplay.test.ts` | Wave result display helper가 legacy card items와 Preact snapshot stat labels/tone을 같은 source로 유지하는지 검증 |
| `src/ui/gameOverDisplay.test.ts` | Game-over display helper가 legacy stat lines와 Preact stat card id/value/tone/order를 같은 source로 유지하는지 검증 |
| `src/ui/tutorialPanelDisplay.test.ts` | Tutorial panel display helper가 legacy panel input과 Preact step/open/detail snapshot을 같은 source로 유지하는지 검증 |
| `src/ui/topHudDisplay.test.ts` | Top HUD display helper가 score/packets/silicon/power/wave timer legacy payload와 Preact snapshot을 같은 source로 유지하는지 검증 |
| `src/ui/tacticalPanelDisplay.test.ts` | Tactical panel display helper가 objective/wave/defense/power legacy payload와 Preact RightRail snapshot을 같은 source로 유지하고 wave timer clamp를 적용하는지 검증 |
| `src/ui/mainMenuDisplay.test.ts` | Main menu display helper가 difficulty options, key hints, save/tutorial status snapshot을 표시 입력과 맞춰 생성하는지 검증 |
| `src/ui/HudApp.test.ts` | Preact HUD root가 MainMenu/TopBar/RightRail/BuildConsole/모달/알림/튜토리얼/모바일 표면을 한 overlay 아래에 모두 조합하고 scene-count 메타를 유지하는지 검증 |
| `src/ui/uiInfrastructure.test.ts` | Preact/signals dependency, `@preact/preset-vite`, Vite manual chunks, TSX `jsxImportSource`, CSS module declaration 등 Phase 0 UI 인프라 계약을 검증 |
| `src/ui/uiDesignTokens.test.ts` | `tokens.css`의 tactical hologram palette, Rajdhani/Share Tech Mono font tokens, spacing/radius/shadow/transition scale, legacy alias 변수, `#preact-hud` fixed/pointer-events-none overlay 계약을 검증 |
| `src/ui/uiBoundary.test.ts` | UI migration 중 runtime manager/InputController가 `scene.uiManager` 직접 호출이나 제거된 `src/managers/*UI*` re-export stub에 다시 의존하지 않는지, MainMenuScene이 visible Phaser Text 메뉴로 회귀하지 않고 Preact menu snapshot/request plus coordinate fallback zones만 유지하는지, `index.html`/`main.css`가 static legacy HUD markup/styles 없이 최소 shell로 남는지 source boundary로 검증 |
| `src/ui/TopHudController.test.ts` | Top HUD EventBus owner listener가 Scene shutdown 때 정리되어 후속 HUD 이벤트를 중복 처리하지 않는지 검증 |
| `src/ui/BuildConsoleController.test.ts` | Build console controller가 refresh/category/tool/hotkey EventBus request를 legacy render/selection mirror와 `BUILD_CONSOLE_UPDATED` snapshot publish 경로로 연결하고 shutdown cleanup하는지 검증 |
| `src/ui/SettingsController.test.ts` | Settings controller가 open/close, speed/FPS/audio/bloom/language/tutorial request, legacy fallback buttons, snapshot publish, language listener cleanup을 검증 |
| `src/ui/GameOverController.test.ts` | Game-over controller가 기존 `GAME_OVER`/action request를 run summary, legacy open/stats/restart mirror, `GAME_OVER_UPDATED` snapshot, reload path, shutdown cleanup으로 연결하는지 검증 |
| `src/ui/NotificationController.test.ts` | Notification controller가 tooltip/activity/wave-result EventBus request를 Preact snapshots, legacy mirrors, activity history state, shutdown cleanup으로 연결하는지 검증 |
| `src/ui/TacticalPanelController.test.ts` | Tactical panel controller가 tactical refresh/frame throttle, wave/power mirror, legacy panel sync, `TACTICAL_PANELS_UPDATED` snapshot publish, shutdown cleanup을 검증 |
| `src/ui/HudShellController.test.ts` | HUD shell controller가 speed/shadow 이벤트, build hotkey 우선 처리, ESC modal close bridge, shutdown listener cleanup을 DOM helper mock 경계에서 검증 |
| `src/ui/HudLocalizationController.test.ts` | HUD localization controller가 setup/languagechange 때 static DOM translation과 build/mobile/tactical refresh request를 발행하고 window listener를 중복 없이 cleanup하는지 검증 |
| `src/ui/MobileActionController.test.ts` | Mobile action controller가 action/cable/summary/status/cancel/rebuild request를 legacy fallback sync, `BUILD_TOOL_SELECT_REQUESTED`, `MOBILE_ACTION_UPDATED` snapshot, Scene shutdown cleanup으로 연결하는지 검증 |
| `src/ui/TrainingLabController.test.ts` | Training Lab controller가 open/tab/auto/job/reward/close/missing-lab request를 legacy fallback sync, `TRAINING_LAB_UPDATED`, activity log, canvas focus restore, shutdown cleanup으로 연결하는지 검증 |
| `src/ui/UIManager.test.ts` | UIManager가 legacy DOM root 보장과 Top HUD refs 수집 후 하위 HUD controllers를 조립하고 setup/setupEvents를 호출하는 얇은 shell 계약을 검증 |
| `tests/e2e/app-smoke.spec.ts` | 시작, 카메라, 설정/언어, 레거시 연구 UI 제거, 배치/케이블/철거, save, 모바일 조작, Preact TopBar/RightRail/BuildConsole/TutorialPanel/Settings/WaveResult/MobileActionBar smoke. Removed ResearchUI is guarded by `#btn-research`/`#research-modal` absence checks while lab shortcuts route through Training Lab. Scene state helper는 selected tool과 mobile action 상태를 `MainScene` mirrors로 읽고, wave/activity/build/tactical smoke는 `window.__GRADIUM_EVENT_BUS__` request hook을 사용해 UIManager private mutator를 직접 호출하지 않는다. 배치 좌표는 시작 Core/Storage footprint와 겹치지 않는 smoke용 타일을 사용 |
| `tests/e2e/tutorial-guidance.spec.ts` | 튜토리얼 힌트 좌표, Preact tutorial startup smoke, 리소스 타일 정합성, 생산/케이블/전력/웨이브/모델 대상 기반 전체 튜토리얼 완료 후 새 캠페인 전환. 데스크톱은 Preact top bar visible 계약을 확인하고, 모바일은 legacy compact `#top-hud` visible + Preact top bar attached 계약을 확인한다. 전환 후 튜토리얼 전용 건물이 남지 않았는지 별도 좌표 집합으로 확인 |
| `tests/e2e/performance.spec.ts` | Preact main menu start fallback과 topbar startup을 거친 뒤 desktop Chromium에서 100/500/1000 건물 fixture를 만들고 `PerformanceStats` summary와 1000 건물 autosave chunk 저장을 검증한 뒤, 성능 샘플을 리셋해 저장 후 p95 frame bound 회복을 별도로 검증 |

## 변경 유형별 추천 테스트

| 변경 영역 | 먼저 실행할 테스트 |
|---|---|
| `src/config.ts` 밸런스/ID | `npm test -- src/config.test.ts`, 관련 utils 테스트 |
| 건물 생산/버퍼 | `src/utils/productionSimulation.test.ts`, 관련 건물 테스트, E2E placement |
| 케이블/AP/Repeater | `src/managers/CableManager.test.ts`, `src/utils/cablePath.test.ts`, `src/utils/apRelay.test.ts`, `tests/e2e/app-smoke.spec.ts` cable tests |
| 전력망/오버레이 | `src/managers/PowerManager.test.ts`, `src/utils/powerPreview.test.ts`, `src/utils/geometry.test.ts`, E2E hotkeys/overlays |
| 웨이브/적/난이도 | `src/utils/waveSimulation.test.ts`, `gridPath.test.ts`, `geometry.test.ts`, `enemyBuildingInteraction.test.ts`, E2E threat panel. 적 이동 변경은 `CONFIG.DIRECTIONS`의 맵 검증 계약과 `BaseEnemy.findPath()`의 적 전용 방향을 분리해서 확인 |
| 저장/로드 | `src/utils/saveMigration.test.ts`, E2E save smoke |
| UI 텍스트/언어 | `src/i18n.test.ts`, `src/ui/HudLocalizationController.test.ts`, `src/ui/SettingsController.test.ts`, E2E language smoke. Settings language controls should update through the named `SettingsController` languagechange handler and clean it up on Scene shutdown, while `HudLocalizationController` handles broader HUD relocalize refresh requests for build/mobile/tactical UI and cleans up its window listener. EventBus owner is `SettingsController` |
| Preact 상단 HUD/shortcuts | `src/ui/topHudDisplay.test.ts`, E2E startup/settings/modal/language smoke. Desktop app smoke verifies visible `preact-top-bar`, `preact-right-rail`, `preact-build-console` and legacy `#hud-top-bar`/`#top-hud`/`#top-actions`/`#hud-right-rail`/`#bottom-ui-container`/`#build-console` hidden shadow state, including disabled/tabindex-suppressed legacy `#btn-settings`; the same smoke verifies shadow `#top-actions` coordinates are ignored by `InputController.isPointerOverDomUI`. Mobile projects keep legacy compact fallback visible and restore `#btn-settings` for fallback compatibility. Legacy `#info-layer` remains attached as a hidden shadow fallback so old shortcut copy does not compete with Preact HUD surfaces. Legacy HUD/settings/game-over/tooltip/activity/notification roots are runtime-created by `src/ui/UIManager.ts`, so selector smoke should wait for scene startup rather than assume static `index.html` markup. Language smoke verifies Preact TopBar labels/aria labels switch through the existing `languagechange` path while legacy HUD text remains attached for compatibility. TopBar labels and resource/power/wave-start/wave-timer HUD values should come from `src/ui/TopHudController.ts` plus `src/ui/topHudDisplay.ts` common display payloads/snapshot helpers so legacy mirrors and Preact snapshots stay aligned, including direct bridge handling of `CORE_DATA_RECEIVED`/`POWER_UPDATED`/`WAVE_STARTED`/`WAVE_UPDATE`. `#preact-topbar-settings`는 `SettingsController` open path를 request로 호출하고, `#preact-topbar-research`/`#preact-topbar-lab`는 `TrainingLabController` open path를 SYSTEM/DEFENSE tab request로 직접 호출하며 기존 `#btn-settings`, `#settings-modal`, `#training-lab-modal` DOM fallback을 유지하는지 확인. Settings open/close/language/speed/save smoke now uses Preact settings controls directly while asserting legacy `#settings-modal` stays attached, hidden, and synced as a shadow fallback. Training Lab smoke verifies research shortcut opens SYSTEM and Lab shortcut opens DEFENSE while legacy `#training-lab-modal` remains a hidden shadow fallback. Preact shortcut buttons는 keyboard focusable하고 i18n-labeled HUD section 안에 있어야 함. StatCard는 `preact-topbar-stats` list와 stable `preact-stat-card/label/value-*` test IDs, label/value aria linkage를 유지해야 하며 data-scan animation은 표시 전용 local state이고 reduced motion에서 비활성 |
| Preact design shell | `src/ui/uiDesignTokens.test.ts`, `npm run build`, E2E startup/perf smoke. Shared `GlassPanel` bevel/scanline/alert pulse는 DOM overlay CSS 전용이며 canvas render나 manager 로직을 건드리지 않는지 확인. Motion-sensitive animation은 `prefers-reduced-motion`에서 비활성. `tokens.css` should keep the tactical hologram palette, font/spacing/radius/shadow/transition tokens, legacy aliases, and `#preact-hud` fixed `pointer-events: none` root so the overlay does not block canvas input by default |
| Preact infrastructure | `src/ui/uiInfrastructure.test.ts`, `npx tsc --noEmit`, `npm run build`. `package.json` should keep `preact` and `@preact/signals`, Vite should keep `@preact/preset-vite` and explicit Phaser/Preact chunks, `tsconfig.json` should keep Preact TSX settings and include `src/**/*.tsx`, and `src/vite-env.d.ts` should keep CSS module typing |
| Preact HUD mount/bridge lifecycle | `src/ui/HudApp.test.ts`, `src/ui/mountHud.test.ts`, `src/ui/signals/bridge.test.ts`, `src/ui/TacticalPanelController.test.ts`, `npx tsc --noEmit`, `npm run build`, E2E startup/language smoke. `HudApp` should keep every Preact HUD surface mounted under the pointer-transparent overlay root, `mountHud` re-mount should tear down the previous Preact root and bridge, Phaser Game `destroy` should unmount the overlay, and `connectGameStateBridge()` should not leave duplicate EventBus owner listeners, window `languagechange` listeners, or wave-result close timers. UI controllers that own HUD/EventBus snapshots (`TopHudController`, `TacticalPanelController`, `BuildConsoleController`, `GameOverController`, `NotificationController`) also register Scene shutdown cleanup for their owner listeners, in addition to `MainScene`'s central owner cleanup list |
| UI migration boundary | `src/ui/uiBoundary.test.ts`, `npx tsc --noEmit`, `npm test`. Runtime managers and `InputController` may emit request/snapshot events, but must not call `scene.uiManager` or depend on removed `src/managers/UIManager.ts`, `SettingsUI.ts`, `TrainingLabUI.ts`, `MobileUIManager.ts` re-export stubs. `MainMenuScene` should keep Phaser background/coordinate fallback zones only, with visible menu state flowing through Preact `MAIN_MENU_*` snapshot/request events rather than Phaser Text. `index.html` should keep only the Phaser mount, `#preact-hud`, font links, and app script; `src/styles/main.css` should stay reset/container-only while legacy HUD selectors live in `legacy-ui.css` and runtime fallback helpers. This guards the project rule that Preact is DOM-overlay only and Phaser/core runtime logic stays decoupled from DOM UI implementation |
| UIManager shell | `src/ui/UIManager.test.ts`, controller-specific tests, E2E startup smoke. `UIManager` should remain a thin composition shell that calls `ensureLegacyHudDom()`, gathers legacy top HUD refs, constructs `TopHudController`/`TacticalPanelController`/`BuildConsoleController`/`HudShellController`/`HudLocalizationController`/modal/mobile/notification controllers, and delegates behavior to their setup methods instead of re-growing direct DOM UI logic |
| Preact modal shell | E2E settings/focus/language/tutorial reset smoke, `src/ui/SettingsController.test.ts`, `npm run build`. `ModalOverlay` overlay click/ESC close는 close request만 발행하고 기존 `#settings-modal` 및 canvas focus restore 경로가 유지되는지 확인. `preact-modal-overlay`/`preact-modal-panel` expose the shell contract, and startup smoke verifies ESC close. `preact-settings-modal` is the visible accessible `role=dialog` surface linked to `preact-settings-title` and `preact-settings-note`; `preact-settings-close` is used by smoke/focus tests; legacy `#settings-modal` remains attached with `data-preact-shadow`/`aria-hidden` while open, keeps language text synced, has shadow controls disabled with `tabindex="-1"`, restores legacy controls after close, and is hidden on close. Settings tabs and shared-button actions should be keyboard-focusable, settings tabs should expose tablist/tab semantics, active settings tabs should link to the visible `tabpanel`, `preact-settings-group-*` surfaces should be labelled by `preact-settings-label-*`, segmented speed/language/FPS controls should expose grouped labels plus `aria-pressed` selected state and stable option test IDs, audio controls should expose a labeled master volume range plus muted `aria-pressed` state, and the bloom/save/load/tutorial controls should expose stable IDs plus their grouped context. Settings labels/open/input values should come from `src/ui/settingsDisplay.ts` common display payload so legacy shadow inputs and Preact snapshot stay aligned; open-only bridge events should use the settings display open-state helper. SettingsModal tutorial reset request는 legacy reset 버튼과 같은 tutorial restart path만 호출하는지 확인. Dim/blur/slide animation은 DOM overlay CSS 전용이며 reduced motion에서 비활성 |
| Preact Training Lab | `src/ui/TrainingLabController.test.ts`, E2E training lab smoke, `npm run build`. `preact-training-lab-modal` is a non-modal dialog linked to visible title plus overview/planner/duration descriptions while legacy `#training-lab-modal` stays attached as a hidden shadow fallback with its controls disabled and removed from tab order while shadowed. Planner status, auto toggle, close button, tablist/tab/tabpanel linkage, row title/detail/tone linkage, data/work progressbars with finite 0-100 value text, per-row reward groups, reward buttons, job select, DEFENSE/SYSTEM tab switching, close request, and canvas focus restore should all keep using the `TrainingLabController`/`ModelTrainingLab` request path. Close should leave the snapshot closed across later background/planner renders until a TopBar or lab open request selects a Lab again and should restore legacy control disabled/tabindex state for fallback compatibility. Snapshot labels and rows should come from `trainingLabDisplay.ts`, not from parsing legacy DOM. EventBus owner is `TrainingLabController` |
| Preact 우측 레일 | `src/ui/tacticalPanelDisplay.test.ts`, `src/ui/TacticalPanelController.test.ts`, E2E startup/threat panel/perf smoke, `npm run build`. Collapse 버튼은 Preact local state이며 keyboard focusable, `aria-expanded`/`aria-controls`, `preact-right-rail-toggle-*` test IDs를 유지하고 기존 `mission-panel`/`threat-panel`/`systems-panel` DOM fallback을 삭제하지 않는지 확인. Startup smoke verifies each visible panel is a labeled `role=region`, collapse removes/restores the body without touching legacy fallback, threat routes expose list semantics, and the threat/power shared ProgressBar surfaces expose localized `role=progressbar` contracts including `preact-right-rail-power-load`. RightRail labels/snapshot은 `TacticalPanelController`가 발행하고 `tacticalPanelDisplay.ts` common display payload에서 나오며 legacy panel text를 다시 읽지 않아야 한다. RightRail labels/snapshot and legacy tactical text should share clamped wave timer text from `formatTacticalWaveTimer()`. Threat meter/routes는 `WaveBriefing` snapshot만 표시하므로 waveSimulation 계산 회귀와 분리 |
| Preact 빌드 콘솔 | E2E startup/interaction/mobile smoke, `npm run build`. PNG icon은 `public/assets/buildings/`에 있는 항목만 snapshot `iconSrc`로 노출하고 없는 build/cable/remove 항목은 swatch fallback을 확인. Preact category/tool controls는 keyboard focusable하고 stable click targets plus `preact-build-category-*`/`preact-build-tool-*` test IDs를 제공하며 active category tabs should link to the visible tool `tabpanel`; desktop and mobile portrait placement/cable/remove smoke now selects tools through these Preact controls and verifies legacy build buttons are disabled with `tabindex="-1"` when shadowed. 실제 선택은 `BuildConsoleController` request 경로가 처리해야 하며 startup, save/load, tutorial lock, training completion refresh 및 E2E 강제 갱신은 `BUILD_CONSOLE_REFRESH_REQUESTED`를 통해 같은 controller render/snapshot 경로에 들어와야 한다. Short landscape에서는 Preact BuildConsole이 숨겨지므로 legacy fallback을 사용하고 legacy buttons remain enabled. Selected tool legacy panel, legacy button label/cost/hotkey/active state, and Preact BuildConsole labels/snapshot은 `buildConsoleSnapshot.ts` common display payload에서 같이 나와야 한다. Tool buttons should keep `aria-describedby="preact-build-console-preview"`, and the preview should expose stable test IDs, `role=status`, polite atomic updates, and list semantics for stat chips. Hover/focus preview와 command strip은 `CONFIG.BUILDINGS`/`CONFIG.CABLES` 및 기존 hotkey 계약 표시만 하므로 build selection/hotkey/canvas placement 회귀를 함께 확인 |
| Preact 빌드 콘솔 controller | `src/ui/BuildConsoleController.test.ts`, E2E startup/interaction/mobile smoke, `npx tsc --noEmit`, `npm run build`. 실제 category/tool/hotkey/refresh 선택은 `BuildConsoleController`가 `BUILD_CATEGORY_SELECT_REQUESTED`/`BUILD_TOOL_SELECT_REQUESTED`/`BUILD_CONSOLE_REFRESH_REQUESTED`로 처리하고, legacy render/selection mirror와 `BUILD_CONSOLE_UPDATED` snapshot이 같은 payload에서 동기화되는지 확인. Controller 변경은 건설 simulation, BuildingManager, WaveManager, CableManager, map generation을 건드리지 않아야 한다 |
| HUD shell controller | `src/ui/HudShellController.test.ts`, E2E startup/settings/focus/build hotkey smoke, `npx tsc --noEmit`, `npm run build`. `HudShellController`가 `HUD_SHELL_SYNC_REQUESTED`/`GAME_SPEED_CHANGED`와 Phaser keyboard event를 받아 legacy shell shadow, speed button mirror, ESC modal close, build hotkey routing, shutdown cleanup을 유지하는지 확인. Controller 변경은 Phaser world rendering/simulation을 건드리지 않아야 한다 |
| HUD localization controller | `src/ui/HudLocalizationController.test.ts`, E2E language/mobile smoke, `npx tsc --noEmit`, `npm run build`. `HudLocalizationController`가 initial `translateStaticDom()`과 `languagechange` listener를 소유하고 `BUILD_CONSOLE_REFRESH_REQUESTED`/`MOBILE_UI_REBUILD_REQUESTED`/`TACTICAL_PANELS_REFRESH_REQUESTED`를 통해 각 owner가 localized 표시 state를 다시 만드는지 확인. Repeated setup은 window listener를 중복 등록하지 않고 Scene shutdown에서 제거해야 한다. Controller 변경은 Phaser simulation을 건드리지 않아야 한다 |
| 메인 메뉴/난이도 | `src/ui/mainMenuDisplay.test.ts`, E2E startup/main-menu smoke. Preact MainMenu overlay가 보이는 메뉴를 담당하며 `#preact-main-menu-start` 우선 클릭, 저장 슬롯이 있을 때 `#preact-main-menu-continue`, root title/subtitle/status/key-hint described linkage, i18n aria/tutorial/save status strip with polite live updates, described difficulty group label and active option description, stable `preact-main-menu-*` test IDs, and key-hint list snapshot 표시, Arrow/Enter/C keyboard shortcuts가 기존 EventBus request만 발행하는지, MainMenuScene에 Phaser Text 메뉴 객체가 남지 않고 투명 Zone coordinate fallback만 유지되는지, difficulty/loadSave 전달, tutorial/campaign mode 분기가 유지되는지 확인. Preact menu labels/options/selected state and key hints should come from `src/ui/mainMenuDisplay.ts`, while `MainMenuScene` only keeps coordinate hit zones as canvas fallback |
| 모바일 조작/CSS | `src/ui/MobileActionController.test.ts`, E2E `mobile-*` projects. PC HUD shell 변경 시 모바일 터치 지점이 HUD/빌드 콘솔에 가로막히지 않는지 확인. Preact MobileActionBar/MobileBuildSummary/BuildConsole 변경 시 labeled mobile action region, toolbar id/label, keyboard/touch focusable controls, action id/label/icon linkage, `aria-pressed`, cable action `aria-controls`/`aria-expanded`/`aria-haspopup`, cable menu `role=menu` plus cable action label linkage, cable option `role=menuitemradio` plus `aria-checked`/`data-selected`/label linkage, MobileBuildSummary `aria-labelledby`/`aria-describedby` atomic live status, `data-has-detail`, handle/content/title/detail stable IDs, grouped summary content linkage, `preact-mobile-action-*`, `preact-mobile-cable-*`, `preact-mobile-build-summary*`, `preact-build-category-*`, `preact-build-tool-*` test IDs, 44px 이상 touch target, icon+label text fit, 기존 `#mobile-action-*`, `#mobile-build-summary`, `#mobile-cable-menu`, `#bottom-ui-container`, `#build-console` 셀렉터와 터치 흐름 유지, short landscape 숨김을 확인. Mobile action/build smoke clicks Preact controls directly when visible and verifies legacy controls are hidden shadow fallbacks with disabled `tabindex="-1"` buttons; short landscape falls back to legacy controls where the Preact surface is intentionally hidden, and still checks legacy controls are enabled plus active classes for compatibility. 모바일 top HUD는 legacy compact fallback이 현재 호환 표면이므로 `tests/e2e/tutorial-guidance.spec.ts`는 Preact top bar를 attached로만 확인하고, visual evidence는 `output/playwright/ui-audit-mobile-portrait.png`와 `output/playwright/ui-audit-mobile-landscape.png`로 중복/겹침을 확인한다. Legacy active state/build summary, transient tool behavior, and Preact mobile labels/snapshot should come from `mobileActionDisplay.ts` common display payload/helpers via `MobileActionController`, including `MOBILE_ACTION_REFRESH_REQUESTED`, `MOBILE_BUILD_SUMMARY_REFRESH_REQUESTED`, language-triggered `MOBILE_UI_REBUILD_REQUESTED`, and cable menu close-on-`BUILDING_SELECTED` paths. Legacy `src/managers/MobileUIManager.ts` re-export stub is removed, so mobile tests should validate behavior and `MobileActionController` EventBus owner cleanup rather than the old file location |
| 툴팁/모바일 정보 시트 | `src/ui/NotificationController.test.ts`, E2E placement/hover smoke. Preact Tooltip 변경 시 기존 `#tooltip`, `#mobile-info-sheet`, `InputController` UI guard와 canvas pointer/touch 흐름이 유지되는지 확인. Desktop tooltip smoke hovers the Core through the real `InputController` path, verifies visible `preact-tooltip`, title/body linkage, header/title/body stable IDs, body `role=list`, line `role=listitem`, and legacy `#tooltip` hidden shadow state. Desktop Preact tooltip 위치 보정, content line normalization, and close label은 snapshot 표시 전용이며 source pointer/world state를 바꾸지 않아야 함. Desktop/mobile legacy mirror input and Preact snapshot should come from `notificationDisplay.ts` tooltip display payloads via `NotificationController`. Preact close는 keyboard focusable `preact-tooltip-close`로 `NotificationController` hide path만 호출해야 함 |
| 모델 훈련/GPU | `src/managers/TrainingPlannerManager.test.ts`, `src/ui/TrainingLabController.test.ts`, `src/utils/modelTrainingProgress.test.ts`, `src/utils/modelTrainingSummary.test.ts`, `src/utils/saveMigration.test.ts`, E2E Training Lab smoke. Preact TrainingLabModal 변경 시 `TrainingLabController` tab/job/auto/reward DOM fallback과 snapshot bridge가 유지되는지 확인. App smoke opens the Lab first through `preact-topbar-research` to verify SYSTEM tab routing, then through `preact-topbar-lab` for DEFENSE, verifies row title/detail linkage plus row data/work `role=progressbar` meters, clicks `preact-training-lab-auto`, verifies its `aria-pressed` state, clicks `preact-training-lab-reward-*`, `preact-training-lab-row-*`, `preact-training-lab-tab-*`, and close, then verifies `TrainingPlannerManager`, defense reward preference, legacy `#training-lab-modal` hidden shadow state with disabled/tabindex-suppressed controls, restored legacy close control after close, and canvas focus. Preact request는 `TrainingLabController` helper와 `ModelTrainingLab`/planner 경로를 재사용해야 하며, side panel이 실제 조작 표면이다. Training Lab side panel should expose a non-modal dialog role with title linkage, keyboard-focusable auto/tab controls, tablist/tab-to-panel semantics, row-labeled data/work progressbars with value text, and reward controls grouped with `aria-pressed` selected state while keeping `preact-training-lab-modal` stable. Legacy shell display and Preact snapshot labels/state should come from `trainingLabDisplay.ts` common display payload, and open-only bridge events should use the Training Lab display open-state helper. Row tone은 snapshot text/progress 표시 전용이라 training planner 계산을 직접 수행하지 않아야 함 |
| 튜토리얼/목표 패널 | `tutorialFlow.test.ts`, `progressionGates.test.ts`, `src/ui/tutorialPanelDisplay.test.ts`, `tests/e2e/tutorial-guidance.spec.ts`, E2E startup panels. 튜토리얼은 우측 정보 레일에 도킹되며 캔버스 고스트/흐름 힌트는 `tutorialFlow.visualHints`, `tutorialFlow.completion`, `TutorialManager` 완료 검사기를 함께 확인. Preact TutorialPanel labels/progress/current objective/typewriter UI는 snapshot 표시 전용이며 legacy `#tutorial-panel`은 hidden shadow fallback으로 sync된다. Tutorial panel open state and Preact step list should come from `tutorialPanelDisplay.ts` display helpers while legacy panel input keeps the same active step/detail/progress source. Legacy `#btn-skip-tutorial` must be disabled with `tabindex="-1"` while shadowed so Preact is the active skip surface. Preact skip button exposes `preact-tutorial-skip`, is described by `preact-tutorial-detail`, and emits `TUTORIAL_SKIP_REQUESTED`, which must reuse the existing `completeAll({ transitionToCampaign: true })` path. Preact panel exposes `preact-tutorial-*` test IDs, title/detail/current `aria-labelledby`/`aria-describedby` linkage, role progressbar value text, current objective group linkage, and role list/listitem step semantics with exactly one `aria-current="step"` item without driving tutorial completion; desktop expects it visible, mobile expects it attached because CSS hides the desktop tutorial mirror below the responsive breakpoint |
| 게임오버/결과 요약 | `runResultSummary.test.ts`, `waveResultSummary.test.ts`, `src/ui/gameOverDisplay.test.ts`, `src/ui/GameOverController.test.ts`, `src/ui/NotificationController.test.ts`, E2E wave/game-over/activity summary. Preact GameOverScreen/WaveResultCard/ActivityLog 변경 시 legacy `#game-over-screen`/`#btn-restart`, `.wave-result-card`, `#activity-log`와 reload/notification/log fallback 경로 유지 확인. Game-over smoke emits the existing `GAME_OVER` event handled by `GameOverController`, verifies the visible `preact-game-over-screen` modal dialog, title/failure `aria-labelledby`/`aria-describedby` linkage, localized integrity label, grouped restart/main-menu action controls with `aria-controls`, integrity/model role progressbars with value text, stat card list semantics, legacy `#game-over-screen` hidden shadow state, disabled/tabindex-suppressed legacy `#btn-restart`, and synced `#game-over-stats`. GameOver legacy stat lines and Preact stat cards should come from `gameOverDisplay.ts` shared stat display helpers, while `GameOverController` should keep the existing `GAME_OVER`/`GAME_OVER_ACTION_REQUESTED` EventBus bridge, legacy restart mirror, reload path, and shutdown cleanup intact. Wave summary runtime and smoke path should enter UI through `WAVE_RESULT_SUMMARY_REQUESTED` handled by `NotificationController`, verifying the same visible atomic `preact-wave-result-card`, title/integrity `aria-labelledby`/`aria-describedby` linkage, localized integrity label/value IDs, close `aria-controls`/`aria-describedby`, role progressbar value/value text, stat label/value linkage, history list and item label/detail linkage, legacy `.wave-result-card` hidden shadow state, and synced summary text. WaveResult legacy card item labels and Preact stat labels/tone should come from `waveResultDisplay.ts` shared stat helpers. Activity log runtime and smoke requests should enter UI through `ACTIVITY_LOG_ENTRY_REQUESTED` plus wave feedback through `WAVE_STARTED`/`WAVE_ENDED`, all handled by `NotificationController`, verifying visible `preact-activity-log` as a title-linked region, title/count/control group/entry IDs, live role log linkage, collapsed recent-entry count, history `aria-expanded` behavior, alert filter `aria-controls`/`aria-pressed` behavior, capped recent history, and legacy `.log-entry` hidden shadow state; Preact labels/snapshot and legacy entry should come from `notificationDisplay.ts` common display payload. Tooltip open/close smoke should keep legacy `#tooltip`/`#mobile-info-sheet` hide state and Preact closed snapshot aligned through `NotificationController` + `notificationDisplay.ts`. Preact GameOver labels/stat cards/integrity/model meters와 WaveResult labels/integrity bar/stat chips/recent timeline은 snapshot 표시 전용이며 GameOver actions는 keyboard focusable `preact-game-over-*` controls로 기존 reload 경로만 호출해야 함. WaveResult close는 keyboard focusable `preact-wave-result-close`로 Preact signal만 닫되 `waveResultDisplay.ts` open-state helper를 사용해야 함. ActivityLog labels/alert/history controls는 snapshot 표시 전용이고 keyboard focusable `preact-activity-log-*` test IDs, alert pressed state, history expanded state, live role log region, and article entries를 제공하지만 기존 log append/remove lifecycle을 바꾸지 않음 |
| 캔버스 그래픽/팔레트 | `src/managers/GridRenderer.test.ts`, `npm run build`, `npm test`, `npx playwright test --workers=1`, 데스크톱 스크린샷. `visualTheme`, `GridRenderer`, `BaseBuilding`, `BaseEnemy`, `CableManager`, `OverlayController`를 함께 확인 |
| 성능/대형 공장 | `npm test`, `npm run build`, `npx playwright test tests/e2e/performance.spec.ts --project=desktop-chromium --workers=1` |

## 새 기능 추가 시 테스트 추가 기준

- 순수 계산으로 분리 가능한 규칙은 먼저 `src/utils/*.test.ts`를 추가합니다.
- 새 config 항목은 `src/config.test.ts`에 최소 계약을 추가합니다.
- 새 저장 필드는 `saveMigration.test.ts`에 구버전/누락 필드 복원 케이스를 추가합니다.
- 새 DOM UI나 실제 조작은 `tests/e2e/app-smoke.spec.ts`에 desktop/mobile 중 필요한 프로젝트만 추가합니다.
- PC 인게임 UI shell 변경은 `#top-hud`, `#mission-panel`, `#threat-panel`, `#bottom-ui-container`, `#tutorial-panel`, 빌드 카테고리 전환, 설정 모달 회귀를 함께 확인합니다.
- 캔버스 그래픽 패치는 테스트가 픽셀 아트를 직접 판정하지 않으므로 1280x720 스크린샷과 DOM 박스 좌표를 증거로 남깁니다.
- 새 건물은 최소한 생성, 비용/해금, 버퍼/생산, 저장 복원 중 위험한 축을 테스트합니다.
- 새 적/웨이브는 `waveSimulation.test.ts`로 수량/HP/브리핑을, `gridPath.test.ts`와 `geometry.test.ts`로 이동 경로/target 중심/blocked fallback을 먼저 고정하고, 필요하면 실제 E2E smoke를 보강합니다.

## 테스트 실패 시 먼저 확인할 부분

- Playwright startup 실패: `playwright.config.ts`의 포트 `5174`, 기존 Vite 서버, 브라우저 설치 여부
- DOM 셀렉터 실패: `index.html`, `src/ui/UIManager.ts`, `main.css`의 id/class/text 변경 여부
- Canvas 클릭 실패: viewport별 좌표, camera zoom/centering, `getScreenPointForTile()` 계산 영향
- Config 테스트 실패: `CONFIG.BUILDINGS` ID와 `BuildingFactory`, `types.ts`, texture 파일 누락
- Save migration 실패: `SaveData` 타입과 `migrateSaveData()` 기본값 불일치
- Wave 테스트 실패: `CONFIG.DIFFICULTY`, `CONFIG.ENEMIES`, `waveSimulation.ts`, `gridPath.ts`, `geometry.ts`, `waveBriefingKey.ts` 계산식 변경 영향
- 언어 테스트 실패: ko/en 키 중 한쪽만 추가했는지 확인

## CI/빌드 검증 흐름

현재 저장소 안에서 별도 CI 설정 파일은 확인하지 못했습니다. 로컬 기준 검증 순서는 다음이 안전합니다.

```powershell
npm test
npm run build
npx playwright test --workers=1
```

문서만 수정한 경우에는 테스트 실행이 필수는 아니지만, 링크/경로 검증과 git diff 확인은 필요합니다.

